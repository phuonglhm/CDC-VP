//
// Automatically Generated SystemC Testbench for ONNX Model
// Model: vit_b-int8.onnx
// Target: Sauria NPU v4.2 SystemC Core (32x32 PE Array, Dual-Lane)
//

#include <systemc.h>
#include <cstdint>
#include <iostream>
#include <fstream>
#include <vector>
#include <iomanip>
#include <cmath>
#include <algorithm>
#include "sauria_types.h"
#include "npu_top.h"

using namespace sauria;

typedef NpuTop<32, 32, int8_t, int8_t, int32_t> NpuInt8T;

SC_MODULE(TbOnnxModel)
{
    sc_in<bool> i_clk;

    sc_signal<bool> rstn, soft_reset, start, done, deadlock;
    sc_signal<uint32_t> mvm_k, host_addr, total_contexts;
    sc_signal<bool> host_wren, host_rden;
    sc_signal<host_data_t> host_wdata, host_rdata;
    sc_signal<host_mask_t> host_wmask;
    sc_signal<float> threshold;
    sc_signal<sc_bv<3>> select;

    NpuInt8T *dut;
    fx1::PerfCounters perf;
    std::vector<uint8_t> dram;
    int errors = 0;

    SC_CTOR(TbOnnxModel)
    {
        dut = new NpuInt8T("dut");
        perf.X = 32;
        perf.Y = 32;
        perf.freq_ghz = 0.8;
        perf.elem_bytes = 1;
        dut->attach_perf(&perf);

        dut->i_clk(i_clk);
        dut->i_rstn(rstn);
        dut->i_soft_reset(soft_reset);
        dut->i_start(start);
        dut->o_done(done);
        dut->o_deadlock(deadlock);
        dut->i_mvm_k(mvm_k);
        dut->i_host_addr(host_addr);
        dut->i_host_wren(host_wren);
        dut->i_host_rden(host_rden);
        dut->i_host_wdata(host_wdata);
        dut->i_host_wmask(host_wmask);
        dut->o_host_rdata(host_rdata);
        dut->i_threshold(threshold);
        dut->i_select(select);
        dut->i_total_contexts(total_contexts);

        SC_THREAD(run);
        sensitive << i_clk.pos();
    }

    void wr(uint32_t addr, uint32_t val)
    {
        host_data_t d;
        d.data.fill(0.0);
        d[0] = static_cast<double>(val);
        host_mask_t m;
        m.data.fill(true);
        host_addr.write(addr);
        host_wdata.write(d);
        host_wmask.write(m);
        host_wren.write(true);
        host_rden.write(false);
        wait();
        host_wren.write(false);
        wait();
    }

    void run()
    {
        dram.resize(static_cast<size_t>(2053) * 1024ULL * 1024ULL, 0); // 2053 MB DRAM buffer
        dut->set_dram(&dram);

        // Pre-load initial DRAM contents from dram_init.bin
        std::ifstream f_init("tools/dram_init.bin", std::ios::binary);
        if (!f_init.is_open()) {
            f_init.open("dram_init.bin", std::ios::binary);
        }
        if (f_init.is_open()) {
            f_init.read(reinterpret_cast<char*>(dram.data()), dram.size());
            f_init.close();
            std::cout << "[TESTBENCH] Successfully loaded DRAM initialization payload (dram_init.bin)" << std::endl;
        } else {
            std::cout << "[WARNING] dram_init.bin not found, running with default zero-initialized DRAM." << std::endl;
        }

        // System Reset
        rstn.write(false);
        soft_reset.write(false);
        start.write(false);
        mvm_k.write(32);
        total_contexts.write(1);
        threshold.write(0.0f);
        select.write(0);
        host_wren.write(false);
        host_rden.write(false);
        wait(5);
        rstn.write(true);
        wait(5);

        std::cout << "==================================================" << std::endl;
        std::cout << "   SAURIA NPU ONNX MODEL EXECUTION BENCHMARK     " << std::endl;
        std::cout << "   Model: vit_b-int8.onnx" << std::endl;
        std::cout << "   Allocated DRAM: 2053 MB" << std::endl;
        std::cout << "   Compiled Instructions: 497" << std::endl;
        std::cout << "==================================================" << std::endl;

        // Queue Compiled Sauria Rich Instructions
        // Instruction 1: /text_model/Sub (Opcode 0x15)
        wr(0x40000444, 0x3CA9200); // r_a_addr
        wr(0x40000448, 0x3CA9200); // r_b_addr
        wr(0x40000408, 0x3CA9600); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 2: /text_model/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x3CA9200); // r_a_addr
        wr(0x40000448, 0x3CA9600); // r_b_addr
        wr(0x40000408, 0x3CACC00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 3: /text_model/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x3CA9A00); // r_a_addr
        wr(0x40000448, 0x3CAF500); // r_b_addr
        wr(0x40000408, 0x3CAF900); // r_out_addr
        wr(0x40000450, 4096); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 4096); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 4: /text_model/Mul (Opcode 0x15)
        wr(0x40000444, 0x3CAD800); // r_a_addr
        wr(0x40000448, 0x3CB0A00); // r_b_addr
        wr(0x40000408, 0x3CB0E00); // r_out_addr
        wr(0x40000450, 4096); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 4096); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 5: /text_model/Add (Opcode 0x15)
        wr(0x40000444, 0x3CB1E00); // r_a_addr
        wr(0x40000448, 0x3CB3B00); // r_b_addr
        wr(0x40000408, 0x3CB3F00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 6: /text_model/embeddings/Add (Opcode 0x15)
        wr(0x40000444, 0x0023E00); // r_a_addr
        wr(0x40000448, 0x1844000); // r_b_addr
        wr(0x40000408, 0x3CB4300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 39424); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 7: /text_model/Sub_1 (Opcode 0x15)
        wr(0x40000444, 0x3D18300); // r_a_addr
        wr(0x40000448, 0x3CA8E00); // r_b_addr
        wr(0x40000408, 0x3D18700); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 8: /text_model/encoder/layers.0/layer_norm1/Sub (Opcode 0x15)
        wr(0x40000444, 0x3CB4300); // r_a_addr
        wr(0x40000448, 0x3CB4300); // r_b_addr
        wr(0x40000408, 0x3D18B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 9: /text_model/encoder/layers.0/layer_norm1/Add (Opcode 0x15)
        wr(0x40000444, 0x3D18B00); // r_a_addr
        wr(0x40000448, 0x3D7CB00); // r_b_addr
        wr(0x40000408, 0x3D7CF00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 10: /text_model/encoder/layers.0/layer_norm1/Div (Opcode 0x15)
        wr(0x40000444, 0x3D18B00); // r_a_addr
        wr(0x40000448, 0x3D7CF00); // r_b_addr
        wr(0x40000408, 0x3DE0F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 11: /text_model/encoder/layers.0/layer_norm1/Mul (Opcode 0x15)
        wr(0x40000444, 0x3DE0F00); // r_a_addr
        wr(0x40000448, 0x0010800); // r_b_addr
        wr(0x40000408, 0x3E44F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 12: /text_model/encoder/layers.0/layer_norm1/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x3E44F00); // r_a_addr
        wr(0x40000448, 0x0010A00); // r_b_addr
        wr(0x40000408, 0x3EA8F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 13: /text_model/encoder/layers.0/self_attn/q_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x3F0CF00); // r_a_addr
        wr(0x40000448, 0x184DA00); // r_b_addr
        wr(0x40000408, 0x3F0D300); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 14: /text_model/encoder/layers.0/self_attn/k_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x3F0CF00); // r_a_addr
        wr(0x40000448, 0x188DE00); // r_b_addr
        wr(0x40000408, 0x3F0D700); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 15: /text_model/encoder/layers.0/self_attn/v_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x3F0CF00); // r_a_addr
        wr(0x40000448, 0x18CE200); // r_b_addr
        wr(0x40000408, 0x3F0DB00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 16: /text_model/encoder/layers.0/self_attn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x3EA8F00); // r_a_addr
        wr(0x40000448, 0x3F0DF00); // r_b_addr
        wr(0x40000408, 0x3F0E300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 17: /text_model/encoder/layers.0/self_attn/q_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x3EA8F00); // r_a_addr
        wr(0x40000448, 0x3F0D300); // r_b_addr
        wr(0x40000408, 0x4300D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 18: /text_model/encoder/layers.0/self_attn/k_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x3EA8F00); // r_a_addr
        wr(0x40000448, 0x3F0D700); // r_b_addr
        wr(0x40000408, 0x4364D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 19: /text_model/encoder/layers.0/self_attn/v_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x3EA8F00); // r_a_addr
        wr(0x40000448, 0x3F0DB00); // r_b_addr
        wr(0x40000408, 0x43C8D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 20: /text_model/encoder/layers.0/self_attn/q_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0010400); // r_a_addr
        wr(0x40000448, 0x4300D00); // r_b_addr
        wr(0x40000408, 0x455F300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 21: /text_model/encoder/layers.0/self_attn/k_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0010000); // r_a_addr
        wr(0x40000448, 0x4364D00); // r_b_addr
        wr(0x40000408, 0x45C3300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 22: /text_model/encoder/layers.0/self_attn/v_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0010200); // r_a_addr
        wr(0x40000448, 0x43C8D00); // r_b_addr
        wr(0x40000408, 0x4627300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 23: /text_model/encoder/layers.0/self_attn/Mul (Opcode 0x15)
        wr(0x40000444, 0x455F300); // r_a_addr
        wr(0x40000448, 0x468B300); // r_b_addr
        wr(0x40000408, 0x468B700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 24: /text_model/encoder/layers.0/self_attn/MatMul (Opcode 0x12)
        wr(0x40000400, 0x468B700); // r_in_addr
        wr(0x40000404, 0x45C3300); // r_w_addr
        wr(0x40000408, 0x46EF700); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 25: /text_model/encoder/layers.0/self_attn/Add (Opcode 0x15)
        wr(0x40000444, 0x46EF700); // r_a_addr
        wr(0x40000448, 0x3CB1E00); // r_b_addr
        wr(0x40000408, 0x8BA1900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 26: /text_model/encoder/layers.0/self_attn/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x8BA1900); // r_a_addr
        wr(0x40000448, 0x3D18700); // r_b_addr
        wr(0x40000408, 0x8C05900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 27: /text_model/encoder/layers.0/self_attn/Softmax (Opcode 0x13)
        wr(0x40000444, 0x8C05900); // r_q_addr
        wr(0x40000448, 0x8C05900); // r_k_addr
        wr(0x4000044C, 0x8C05900); // r_v_addr
        wr(0x40000408, 0x8C69900); // r_out_addr
        wr(0x40000450, 4096); // r_seq_len
        wr(0x40000454, 1); // r_num_heads
        wr(0x40000458, 4096); // r_head_dim
        wr(0x40000310, 0x13);

        // Instruction 28: /text_model/encoder/layers.0/self_attn/MatMul_1 (Opcode 0x12)
        wr(0x40000400, 0x8C69900); // r_in_addr
        wr(0x40000404, 0x4627300); // r_w_addr
        wr(0x40000408, 0x9C69900); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 29: /text_model/encoder/layers.0/self_attn/out_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0xDC69900); // r_a_addr
        wr(0x40000448, 0x190E600); // r_b_addr
        wr(0x40000408, 0xDC69D00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 30: /text_model/encoder/layers.0/self_attn/out_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x9C69900); // r_a_addr
        wr(0x40000448, 0xDC69D00); // r_b_addr
        wr(0x40000408, 0xDC6A100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 31: /text_model/encoder/layers.0/self_attn/out_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0010600); // r_a_addr
        wr(0x40000448, 0xDC6A100); // r_b_addr
        wr(0x40000408, 0xDCCE100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 32: /text_model/encoder/layers.0/Add (Opcode 0x15)
        wr(0x40000444, 0x3CB4300); // r_a_addr
        wr(0x40000448, 0xDCCE100); // r_b_addr
        wr(0x40000408, 0xDD32100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 33: /text_model/encoder/layers.0/layer_norm2/Sub (Opcode 0x15)
        wr(0x40000444, 0xDD32100); // r_a_addr
        wr(0x40000448, 0xDD32100); // r_b_addr
        wr(0x40000408, 0xDD96100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 34: /text_model/encoder/layers.0/layer_norm2/Add (Opcode 0x15)
        wr(0x40000444, 0xDD96100); // r_a_addr
        wr(0x40000448, 0xDDFA100); // r_b_addr
        wr(0x40000408, 0xDDFA500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 35: /text_model/encoder/layers.0/layer_norm2/Div (Opcode 0x15)
        wr(0x40000444, 0xDD96100); // r_a_addr
        wr(0x40000448, 0xDDFA500); // r_b_addr
        wr(0x40000408, 0xDE5E500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 36: /text_model/encoder/layers.0/layer_norm2/Mul (Opcode 0x15)
        wr(0x40000444, 0xDE5E500); // r_a_addr
        wr(0x40000448, 0x0011600); // r_b_addr
        wr(0x40000408, 0xDEC2500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 37: /text_model/encoder/layers.0/layer_norm2/Add_1 (Opcode 0x15)
        wr(0x40000444, 0xDEC2500); // r_a_addr
        wr(0x40000448, 0x0011800); // r_b_addr
        wr(0x40000408, 0xDF26500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 38: /text_model/encoder/layers.0/mlp/fc1/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0xDF8A500); // r_a_addr
        wr(0x40000448, 0x194EA00); // r_b_addr
        wr(0x40000408, 0xDF8A900); // r_out_addr
        wr(0x40000450, 2048); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 39: /text_model/encoder/layers.0/mlp/fc1/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0xDF26500); // r_a_addr
        wr(0x40000448, 0xDF8A900); // r_b_addr
        wr(0x40000408, 0xDF8B100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 40: /text_model/encoder/layers.0/mlp/fc1/Add (Opcode 0x15)
        wr(0x40000444, 0x0010C00); // r_a_addr
        wr(0x40000448, 0xDF8B100); // r_b_addr
        wr(0x40000408, 0xDFEF100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 2048); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 41: /text_model/encoder/layers.0/mlp/activation_fn/Mul (Opcode 0x15)
        wr(0x40000444, 0xDFEF100); // r_a_addr
        wr(0x40000448, 0xE053100); // r_b_addr
        wr(0x40000408, 0xE053500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 42: /text_model/encoder/layers.0/mlp/activation_fn/Sigmoid (Opcode 0x15)
        wr(0x40000444, 0xE053500); // r_a_addr
        wr(0x40000448, 0xE053500); // r_b_addr
        wr(0x40000408, 0xE0B7500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 43: /text_model/encoder/layers.0/mlp/activation_fn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0xDFEF100); // r_a_addr
        wr(0x40000448, 0xE0B7500); // r_b_addr
        wr(0x40000408, 0xE11B500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 44: /text_model/encoder/layers.0/mlp/fc2/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0xE17F500); // r_a_addr
        wr(0x40000448, 0x1A4FA00); // r_b_addr
        wr(0x40000408, 0xE17F900); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 45: /text_model/encoder/layers.0/mlp/fc2/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0xE11B500); // r_a_addr
        wr(0x40000448, 0xE17F900); // r_b_addr
        wr(0x40000408, 0xE17FD00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 46: /text_model/encoder/layers.0/mlp/fc2/Add (Opcode 0x15)
        wr(0x40000444, 0x0011400); // r_a_addr
        wr(0x40000448, 0xE17FD00); // r_b_addr
        wr(0x40000408, 0xE1E3D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 47: /text_model/encoder/layers.0/Add_1 (Opcode 0x15)
        wr(0x40000444, 0xDD32100); // r_a_addr
        wr(0x40000448, 0xE1E3D00); // r_b_addr
        wr(0x40000408, 0xE247D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 48: /text_model/encoder/layers.1/layer_norm1/Sub (Opcode 0x15)
        wr(0x40000444, 0xE247D00); // r_a_addr
        wr(0x40000448, 0xE247D00); // r_b_addr
        wr(0x40000408, 0xE2ABD00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 49: /text_model/encoder/layers.1/layer_norm1/Add (Opcode 0x15)
        wr(0x40000444, 0xE2ABD00); // r_a_addr
        wr(0x40000448, 0xE30FD00); // r_b_addr
        wr(0x40000408, 0xE310100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 50: /text_model/encoder/layers.1/layer_norm1/Div (Opcode 0x15)
        wr(0x40000444, 0xE2ABD00); // r_a_addr
        wr(0x40000448, 0xE310100); // r_b_addr
        wr(0x40000408, 0xE374100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 51: /text_model/encoder/layers.1/layer_norm1/Mul (Opcode 0x15)
        wr(0x40000444, 0xE374100); // r_a_addr
        wr(0x40000448, 0x0012200); // r_b_addr
        wr(0x40000408, 0xE3D8100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 52: /text_model/encoder/layers.1/layer_norm1/Add_1 (Opcode 0x15)
        wr(0x40000444, 0xE3D8100); // r_a_addr
        wr(0x40000448, 0x0012400); // r_b_addr
        wr(0x40000408, 0xE43C100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 53: /text_model/encoder/layers.1/self_attn/q_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0xE4A0100); // r_a_addr
        wr(0x40000448, 0x1B4FE00); // r_b_addr
        wr(0x40000408, 0xE4A0500); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 54: /text_model/encoder/layers.1/self_attn/k_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0xE4A0100); // r_a_addr
        wr(0x40000448, 0x1B90200); // r_b_addr
        wr(0x40000408, 0xE4A0900); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 55: /text_model/encoder/layers.1/self_attn/v_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0xE4A0100); // r_a_addr
        wr(0x40000448, 0x1BD0600); // r_b_addr
        wr(0x40000408, 0xE4A0D00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 56: /text_model/encoder/layers.1/self_attn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0xE43C100); // r_a_addr
        wr(0x40000448, 0xE4A1100); // r_b_addr
        wr(0x40000408, 0xE4A1500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 57: /text_model/encoder/layers.1/self_attn/q_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0xE43C100); // r_a_addr
        wr(0x40000448, 0xE4A0500); // r_b_addr
        wr(0x40000408, 0xE893F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 58: /text_model/encoder/layers.1/self_attn/k_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0xE43C100); // r_a_addr
        wr(0x40000448, 0xE4A0900); // r_b_addr
        wr(0x40000408, 0xE8F7F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 59: /text_model/encoder/layers.1/self_attn/v_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0xE43C100); // r_a_addr
        wr(0x40000448, 0xE4A0D00); // r_b_addr
        wr(0x40000408, 0xE95BF00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 60: /text_model/encoder/layers.1/self_attn/q_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0011E00); // r_a_addr
        wr(0x40000448, 0xE893F00); // r_b_addr
        wr(0x40000408, 0xEAF2500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 61: /text_model/encoder/layers.1/self_attn/k_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0011A00); // r_a_addr
        wr(0x40000448, 0xE8F7F00); // r_b_addr
        wr(0x40000408, 0xEB56500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 62: /text_model/encoder/layers.1/self_attn/v_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0011C00); // r_a_addr
        wr(0x40000448, 0xE95BF00); // r_b_addr
        wr(0x40000408, 0xEBBA500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 63: /text_model/encoder/layers.1/self_attn/Mul (Opcode 0x15)
        wr(0x40000444, 0xEAF2500); // r_a_addr
        wr(0x40000448, 0xEC1E500); // r_b_addr
        wr(0x40000408, 0xEC1E900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 64: /text_model/encoder/layers.1/self_attn/MatMul (Opcode 0x12)
        wr(0x40000400, 0xEC1E900); // r_in_addr
        wr(0x40000404, 0xEB56500); // r_w_addr
        wr(0x40000408, 0xEC82900); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 65: /text_model/encoder/layers.1/self_attn/Add (Opcode 0x15)
        wr(0x40000444, 0xEC82900); // r_a_addr
        wr(0x40000448, 0x3CB1E00); // r_b_addr
        wr(0x40000408, 0x13134B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 66: /text_model/encoder/layers.1/self_attn/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x13134B00); // r_a_addr
        wr(0x40000448, 0x3D18700); // r_b_addr
        wr(0x40000408, 0x13198B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 67: /text_model/encoder/layers.1/self_attn/Softmax (Opcode 0x13)
        wr(0x40000444, 0x13198B00); // r_q_addr
        wr(0x40000448, 0x13198B00); // r_k_addr
        wr(0x4000044C, 0x13198B00); // r_v_addr
        wr(0x40000408, 0x131FCB00); // r_out_addr
        wr(0x40000450, 4096); // r_seq_len
        wr(0x40000454, 1); // r_num_heads
        wr(0x40000458, 4096); // r_head_dim
        wr(0x40000310, 0x13);

        // Instruction 68: /text_model/encoder/layers.1/self_attn/MatMul_1 (Opcode 0x12)
        wr(0x40000400, 0x131FCB00); // r_in_addr
        wr(0x40000404, 0xEBBA500); // r_w_addr
        wr(0x40000408, 0x141FCB00); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 69: /text_model/encoder/layers.1/self_attn/out_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x181FCB00); // r_a_addr
        wr(0x40000448, 0x1C10A00); // r_b_addr
        wr(0x40000408, 0x181FCF00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 70: /text_model/encoder/layers.1/self_attn/out_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x141FCB00); // r_a_addr
        wr(0x40000448, 0x181FCF00); // r_b_addr
        wr(0x40000408, 0x181FD300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 71: /text_model/encoder/layers.1/self_attn/out_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0012000); // r_a_addr
        wr(0x40000448, 0x181FD300); // r_b_addr
        wr(0x40000408, 0x18261300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 72: /text_model/encoder/layers.1/Add (Opcode 0x15)
        wr(0x40000444, 0xE247D00); // r_a_addr
        wr(0x40000448, 0x18261300); // r_b_addr
        wr(0x40000408, 0x182C5300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 73: /text_model/encoder/layers.1/layer_norm2/Sub (Opcode 0x15)
        wr(0x40000444, 0x182C5300); // r_a_addr
        wr(0x40000448, 0x182C5300); // r_b_addr
        wr(0x40000408, 0x18329300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 74: /text_model/encoder/layers.1/layer_norm2/Add (Opcode 0x15)
        wr(0x40000444, 0x18329300); // r_a_addr
        wr(0x40000448, 0x1838D300); // r_b_addr
        wr(0x40000408, 0x1838D700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 75: /text_model/encoder/layers.1/layer_norm2/Div (Opcode 0x15)
        wr(0x40000444, 0x18329300); // r_a_addr
        wr(0x40000448, 0x1838D700); // r_b_addr
        wr(0x40000408, 0x183F1700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 76: /text_model/encoder/layers.1/layer_norm2/Mul (Opcode 0x15)
        wr(0x40000444, 0x183F1700); // r_a_addr
        wr(0x40000448, 0x0013000); // r_b_addr
        wr(0x40000408, 0x18455700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 77: /text_model/encoder/layers.1/layer_norm2/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x18455700); // r_a_addr
        wr(0x40000448, 0x0013200); // r_b_addr
        wr(0x40000408, 0x184B9700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 78: /text_model/encoder/layers.1/mlp/fc1/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x1851D700); // r_a_addr
        wr(0x40000448, 0x1C50E00); // r_b_addr
        wr(0x40000408, 0x1851DB00); // r_out_addr
        wr(0x40000450, 2048); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 79: /text_model/encoder/layers.1/mlp/fc1/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x184B9700); // r_a_addr
        wr(0x40000448, 0x1851DB00); // r_b_addr
        wr(0x40000408, 0x1851E300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 80: /text_model/encoder/layers.1/mlp/fc1/Add (Opcode 0x15)
        wr(0x40000444, 0x0012600); // r_a_addr
        wr(0x40000448, 0x1851E300); // r_b_addr
        wr(0x40000408, 0x18582300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 2048); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 81: /text_model/encoder/layers.1/mlp/activation_fn/Mul (Opcode 0x15)
        wr(0x40000444, 0x18582300); // r_a_addr
        wr(0x40000448, 0x185E6300); // r_b_addr
        wr(0x40000408, 0x185E6700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 82: /text_model/encoder/layers.1/mlp/activation_fn/Sigmoid (Opcode 0x15)
        wr(0x40000444, 0x185E6700); // r_a_addr
        wr(0x40000448, 0x185E6700); // r_b_addr
        wr(0x40000408, 0x1864A700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 83: /text_model/encoder/layers.1/mlp/activation_fn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x18582300); // r_a_addr
        wr(0x40000448, 0x1864A700); // r_b_addr
        wr(0x40000408, 0x186AE700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 84: /text_model/encoder/layers.1/mlp/fc2/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x18712700); // r_a_addr
        wr(0x40000448, 0x1D51E00); // r_b_addr
        wr(0x40000408, 0x18712B00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 85: /text_model/encoder/layers.1/mlp/fc2/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x186AE700); // r_a_addr
        wr(0x40000448, 0x18712B00); // r_b_addr
        wr(0x40000408, 0x18712F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 86: /text_model/encoder/layers.1/mlp/fc2/Add (Opcode 0x15)
        wr(0x40000444, 0x0012E00); // r_a_addr
        wr(0x40000448, 0x18712F00); // r_b_addr
        wr(0x40000408, 0x18776F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 87: /text_model/encoder/layers.1/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x182C5300); // r_a_addr
        wr(0x40000448, 0x18776F00); // r_b_addr
        wr(0x40000408, 0x187DAF00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 88: /text_model/encoder/layers.2/layer_norm1/Sub (Opcode 0x15)
        wr(0x40000444, 0x187DAF00); // r_a_addr
        wr(0x40000448, 0x187DAF00); // r_b_addr
        wr(0x40000408, 0x1883EF00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 89: /text_model/encoder/layers.2/layer_norm1/Add (Opcode 0x15)
        wr(0x40000444, 0x1883EF00); // r_a_addr
        wr(0x40000448, 0x188A2F00); // r_b_addr
        wr(0x40000408, 0x188A3300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 90: /text_model/encoder/layers.2/layer_norm1/Div (Opcode 0x15)
        wr(0x40000444, 0x1883EF00); // r_a_addr
        wr(0x40000448, 0x188A3300); // r_b_addr
        wr(0x40000408, 0x18907300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 91: /text_model/encoder/layers.2/layer_norm1/Mul (Opcode 0x15)
        wr(0x40000444, 0x18907300); // r_a_addr
        wr(0x40000448, 0x0013C00); // r_b_addr
        wr(0x40000408, 0x1896B300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 92: /text_model/encoder/layers.2/layer_norm1/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x1896B300); // r_a_addr
        wr(0x40000448, 0x0013E00); // r_b_addr
        wr(0x40000408, 0x189CF300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 93: /text_model/encoder/layers.2/self_attn/q_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x18A33300); // r_a_addr
        wr(0x40000448, 0x1E52200); // r_b_addr
        wr(0x40000408, 0x18A33700); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 94: /text_model/encoder/layers.2/self_attn/k_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x18A33300); // r_a_addr
        wr(0x40000448, 0x1E92600); // r_b_addr
        wr(0x40000408, 0x18A33B00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 95: /text_model/encoder/layers.2/self_attn/v_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x18A33300); // r_a_addr
        wr(0x40000448, 0x1ED2A00); // r_b_addr
        wr(0x40000408, 0x18A33F00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 96: /text_model/encoder/layers.2/self_attn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x189CF300); // r_a_addr
        wr(0x40000448, 0x18A34300); // r_b_addr
        wr(0x40000408, 0x18A34700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 97: /text_model/encoder/layers.2/self_attn/q_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x189CF300); // r_a_addr
        wr(0x40000448, 0x18A33700); // r_b_addr
        wr(0x40000408, 0x18E27100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 98: /text_model/encoder/layers.2/self_attn/k_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x189CF300); // r_a_addr
        wr(0x40000448, 0x18A33B00); // r_b_addr
        wr(0x40000408, 0x18E8B100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 99: /text_model/encoder/layers.2/self_attn/v_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x189CF300); // r_a_addr
        wr(0x40000448, 0x18A33F00); // r_b_addr
        wr(0x40000408, 0x18EEF100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 100: /text_model/encoder/layers.2/self_attn/q_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0013800); // r_a_addr
        wr(0x40000448, 0x18E27100); // r_b_addr
        wr(0x40000408, 0x19085700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 101: /text_model/encoder/layers.2/self_attn/k_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0013400); // r_a_addr
        wr(0x40000448, 0x18E8B100); // r_b_addr
        wr(0x40000408, 0x190E9700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 102: /text_model/encoder/layers.2/self_attn/v_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0013600); // r_a_addr
        wr(0x40000448, 0x18EEF100); // r_b_addr
        wr(0x40000408, 0x1914D700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 103: /text_model/encoder/layers.2/self_attn/Mul (Opcode 0x15)
        wr(0x40000444, 0x19085700); // r_a_addr
        wr(0x40000448, 0x191B1700); // r_b_addr
        wr(0x40000408, 0x191B1B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 104: /text_model/encoder/layers.2/self_attn/MatMul (Opcode 0x12)
        wr(0x40000400, 0x191B1B00); // r_in_addr
        wr(0x40000404, 0x190E9700); // r_w_addr
        wr(0x40000408, 0x19215B00); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 105: /text_model/encoder/layers.2/self_attn/Add (Opcode 0x15)
        wr(0x40000444, 0x19215B00); // r_a_addr
        wr(0x40000448, 0x3CB1E00); // r_b_addr
        wr(0x40000408, 0x1D6C7D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 106: /text_model/encoder/layers.2/self_attn/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x1D6C7D00); // r_a_addr
        wr(0x40000448, 0x3D18700); // r_b_addr
        wr(0x40000408, 0x1D72BD00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 107: /text_model/encoder/layers.2/self_attn/Softmax (Opcode 0x13)
        wr(0x40000444, 0x1D72BD00); // r_q_addr
        wr(0x40000448, 0x1D72BD00); // r_k_addr
        wr(0x4000044C, 0x1D72BD00); // r_v_addr
        wr(0x40000408, 0x1D78FD00); // r_out_addr
        wr(0x40000450, 4096); // r_seq_len
        wr(0x40000454, 1); // r_num_heads
        wr(0x40000458, 4096); // r_head_dim
        wr(0x40000310, 0x13);

        // Instruction 108: /text_model/encoder/layers.2/self_attn/MatMul_1 (Opcode 0x12)
        wr(0x40000400, 0x1D78FD00); // r_in_addr
        wr(0x40000404, 0x1914D700); // r_w_addr
        wr(0x40000408, 0x1E78FD00); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 109: /text_model/encoder/layers.2/self_attn/out_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x2278FD00); // r_a_addr
        wr(0x40000448, 0x1F12E00); // r_b_addr
        wr(0x40000408, 0x22790100); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 110: /text_model/encoder/layers.2/self_attn/out_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x1E78FD00); // r_a_addr
        wr(0x40000448, 0x22790100); // r_b_addr
        wr(0x40000408, 0x22790500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 111: /text_model/encoder/layers.2/self_attn/out_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0013A00); // r_a_addr
        wr(0x40000448, 0x22790500); // r_b_addr
        wr(0x40000408, 0x227F4500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 112: /text_model/encoder/layers.2/Add (Opcode 0x15)
        wr(0x40000444, 0x187DAF00); // r_a_addr
        wr(0x40000448, 0x227F4500); // r_b_addr
        wr(0x40000408, 0x22858500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 113: /text_model/encoder/layers.2/layer_norm2/Sub (Opcode 0x15)
        wr(0x40000444, 0x22858500); // r_a_addr
        wr(0x40000448, 0x22858500); // r_b_addr
        wr(0x40000408, 0x228BC500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 114: /text_model/encoder/layers.2/layer_norm2/Add (Opcode 0x15)
        wr(0x40000444, 0x228BC500); // r_a_addr
        wr(0x40000448, 0x22920500); // r_b_addr
        wr(0x40000408, 0x22920900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 115: /text_model/encoder/layers.2/layer_norm2/Div (Opcode 0x15)
        wr(0x40000444, 0x228BC500); // r_a_addr
        wr(0x40000448, 0x22920900); // r_b_addr
        wr(0x40000408, 0x22984900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 116: /text_model/encoder/layers.2/layer_norm2/Mul (Opcode 0x15)
        wr(0x40000444, 0x22984900); // r_a_addr
        wr(0x40000448, 0x0014A00); // r_b_addr
        wr(0x40000408, 0x229E8900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 117: /text_model/encoder/layers.2/layer_norm2/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x229E8900); // r_a_addr
        wr(0x40000448, 0x0014C00); // r_b_addr
        wr(0x40000408, 0x22A4C900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 118: /text_model/encoder/layers.2/mlp/fc1/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x22AB0900); // r_a_addr
        wr(0x40000448, 0x1F53200); // r_b_addr
        wr(0x40000408, 0x22AB0D00); // r_out_addr
        wr(0x40000450, 2048); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 119: /text_model/encoder/layers.2/mlp/fc1/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x22A4C900); // r_a_addr
        wr(0x40000448, 0x22AB0D00); // r_b_addr
        wr(0x40000408, 0x22AB1500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 120: /text_model/encoder/layers.2/mlp/fc1/Add (Opcode 0x15)
        wr(0x40000444, 0x0014000); // r_a_addr
        wr(0x40000448, 0x22AB1500); // r_b_addr
        wr(0x40000408, 0x22B15500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 2048); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 121: /text_model/encoder/layers.2/mlp/activation_fn/Mul (Opcode 0x15)
        wr(0x40000444, 0x22B15500); // r_a_addr
        wr(0x40000448, 0x22B79500); // r_b_addr
        wr(0x40000408, 0x22B79900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 122: /text_model/encoder/layers.2/mlp/activation_fn/Sigmoid (Opcode 0x15)
        wr(0x40000444, 0x22B79900); // r_a_addr
        wr(0x40000448, 0x22B79900); // r_b_addr
        wr(0x40000408, 0x22BDD900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 123: /text_model/encoder/layers.2/mlp/activation_fn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x22B15500); // r_a_addr
        wr(0x40000448, 0x22BDD900); // r_b_addr
        wr(0x40000408, 0x22C41900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 124: /text_model/encoder/layers.2/mlp/fc2/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x22CA5900); // r_a_addr
        wr(0x40000448, 0x2054200); // r_b_addr
        wr(0x40000408, 0x22CA5D00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 125: /text_model/encoder/layers.2/mlp/fc2/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x22C41900); // r_a_addr
        wr(0x40000448, 0x22CA5D00); // r_b_addr
        wr(0x40000408, 0x22CA6100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 126: /text_model/encoder/layers.2/mlp/fc2/Add (Opcode 0x15)
        wr(0x40000444, 0x0014800); // r_a_addr
        wr(0x40000448, 0x22CA6100); // r_b_addr
        wr(0x40000408, 0x22D0A100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 127: /text_model/encoder/layers.2/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x22858500); // r_a_addr
        wr(0x40000448, 0x22D0A100); // r_b_addr
        wr(0x40000408, 0x22D6E100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 128: /text_model/encoder/layers.3/layer_norm1/Sub (Opcode 0x15)
        wr(0x40000444, 0x22D6E100); // r_a_addr
        wr(0x40000448, 0x22D6E100); // r_b_addr
        wr(0x40000408, 0x22DD2100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 129: /text_model/encoder/layers.3/layer_norm1/Add (Opcode 0x15)
        wr(0x40000444, 0x22DD2100); // r_a_addr
        wr(0x40000448, 0x22E36100); // r_b_addr
        wr(0x40000408, 0x22E36500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 130: /text_model/encoder/layers.3/layer_norm1/Div (Opcode 0x15)
        wr(0x40000444, 0x22DD2100); // r_a_addr
        wr(0x40000448, 0x22E36500); // r_b_addr
        wr(0x40000408, 0x22E9A500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 131: /text_model/encoder/layers.3/layer_norm1/Mul (Opcode 0x15)
        wr(0x40000444, 0x22E9A500); // r_a_addr
        wr(0x40000448, 0x0015600); // r_b_addr
        wr(0x40000408, 0x22EFE500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 132: /text_model/encoder/layers.3/layer_norm1/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x22EFE500); // r_a_addr
        wr(0x40000448, 0x0015800); // r_b_addr
        wr(0x40000408, 0x22F62500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 133: /text_model/encoder/layers.3/self_attn/q_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x22FC6500); // r_a_addr
        wr(0x40000448, 0x2154600); // r_b_addr
        wr(0x40000408, 0x22FC6900); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 134: /text_model/encoder/layers.3/self_attn/k_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x22FC6500); // r_a_addr
        wr(0x40000448, 0x2194A00); // r_b_addr
        wr(0x40000408, 0x22FC6D00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 135: /text_model/encoder/layers.3/self_attn/v_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x22FC6500); // r_a_addr
        wr(0x40000448, 0x21D4E00); // r_b_addr
        wr(0x40000408, 0x22FC7100); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 136: /text_model/encoder/layers.3/self_attn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x22F62500); // r_a_addr
        wr(0x40000448, 0x22FC7500); // r_b_addr
        wr(0x40000408, 0x22FC7900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 137: /text_model/encoder/layers.3/self_attn/q_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x22F62500); // r_a_addr
        wr(0x40000448, 0x22FC6900); // r_b_addr
        wr(0x40000408, 0x233BA300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 138: /text_model/encoder/layers.3/self_attn/k_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x22F62500); // r_a_addr
        wr(0x40000448, 0x22FC6D00); // r_b_addr
        wr(0x40000408, 0x2341E300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 139: /text_model/encoder/layers.3/self_attn/v_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x22F62500); // r_a_addr
        wr(0x40000448, 0x22FC7100); // r_b_addr
        wr(0x40000408, 0x23482300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 140: /text_model/encoder/layers.3/self_attn/q_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0015200); // r_a_addr
        wr(0x40000448, 0x233BA300); // r_b_addr
        wr(0x40000408, 0x23618900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 141: /text_model/encoder/layers.3/self_attn/k_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0014E00); // r_a_addr
        wr(0x40000448, 0x2341E300); // r_b_addr
        wr(0x40000408, 0x2367C900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 142: /text_model/encoder/layers.3/self_attn/v_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0015000); // r_a_addr
        wr(0x40000448, 0x23482300); // r_b_addr
        wr(0x40000408, 0x236E0900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 143: /text_model/encoder/layers.3/self_attn/Mul (Opcode 0x15)
        wr(0x40000444, 0x23618900); // r_a_addr
        wr(0x40000448, 0x23744900); // r_b_addr
        wr(0x40000408, 0x23744D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 144: /text_model/encoder/layers.3/self_attn/MatMul (Opcode 0x12)
        wr(0x40000400, 0x23744D00); // r_in_addr
        wr(0x40000404, 0x2367C900); // r_w_addr
        wr(0x40000408, 0x237A8D00); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 145: /text_model/encoder/layers.3/self_attn/Add (Opcode 0x15)
        wr(0x40000444, 0x237A8D00); // r_a_addr
        wr(0x40000448, 0x3CB1E00); // r_b_addr
        wr(0x40000408, 0x27C5AF00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 146: /text_model/encoder/layers.3/self_attn/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x27C5AF00); // r_a_addr
        wr(0x40000448, 0x3D18700); // r_b_addr
        wr(0x40000408, 0x27CBEF00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 147: /text_model/encoder/layers.3/self_attn/Softmax (Opcode 0x13)
        wr(0x40000444, 0x27CBEF00); // r_q_addr
        wr(0x40000448, 0x27CBEF00); // r_k_addr
        wr(0x4000044C, 0x27CBEF00); // r_v_addr
        wr(0x40000408, 0x27D22F00); // r_out_addr
        wr(0x40000450, 4096); // r_seq_len
        wr(0x40000454, 1); // r_num_heads
        wr(0x40000458, 4096); // r_head_dim
        wr(0x40000310, 0x13);

        // Instruction 148: /text_model/encoder/layers.3/self_attn/MatMul_1 (Opcode 0x12)
        wr(0x40000400, 0x27D22F00); // r_in_addr
        wr(0x40000404, 0x236E0900); // r_w_addr
        wr(0x40000408, 0x28D22F00); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 149: /text_model/encoder/layers.3/self_attn/out_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x2CD22F00); // r_a_addr
        wr(0x40000448, 0x2215200); // r_b_addr
        wr(0x40000408, 0x2CD23300); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 150: /text_model/encoder/layers.3/self_attn/out_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x28D22F00); // r_a_addr
        wr(0x40000448, 0x2CD23300); // r_b_addr
        wr(0x40000408, 0x2CD23700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 151: /text_model/encoder/layers.3/self_attn/out_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0015400); // r_a_addr
        wr(0x40000448, 0x2CD23700); // r_b_addr
        wr(0x40000408, 0x2CD87700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 152: /text_model/encoder/layers.3/Add (Opcode 0x15)
        wr(0x40000444, 0x22D6E100); // r_a_addr
        wr(0x40000448, 0x2CD87700); // r_b_addr
        wr(0x40000408, 0x2CDEB700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 153: /text_model/encoder/layers.3/layer_norm2/Sub (Opcode 0x15)
        wr(0x40000444, 0x2CDEB700); // r_a_addr
        wr(0x40000448, 0x2CDEB700); // r_b_addr
        wr(0x40000408, 0x2CE4F700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 154: /text_model/encoder/layers.3/layer_norm2/Add (Opcode 0x15)
        wr(0x40000444, 0x2CE4F700); // r_a_addr
        wr(0x40000448, 0x2CEB3700); // r_b_addr
        wr(0x40000408, 0x2CEB3B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 155: /text_model/encoder/layers.3/layer_norm2/Div (Opcode 0x15)
        wr(0x40000444, 0x2CE4F700); // r_a_addr
        wr(0x40000448, 0x2CEB3B00); // r_b_addr
        wr(0x40000408, 0x2CF17B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 156: /text_model/encoder/layers.3/layer_norm2/Mul (Opcode 0x15)
        wr(0x40000444, 0x2CF17B00); // r_a_addr
        wr(0x40000448, 0x0016400); // r_b_addr
        wr(0x40000408, 0x2CF7BB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 157: /text_model/encoder/layers.3/layer_norm2/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x2CF7BB00); // r_a_addr
        wr(0x40000448, 0x0016600); // r_b_addr
        wr(0x40000408, 0x2CFDFB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 158: /text_model/encoder/layers.3/mlp/fc1/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x2D043B00); // r_a_addr
        wr(0x40000448, 0x2255600); // r_b_addr
        wr(0x40000408, 0x2D043F00); // r_out_addr
        wr(0x40000450, 2048); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 159: /text_model/encoder/layers.3/mlp/fc1/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x2CFDFB00); // r_a_addr
        wr(0x40000448, 0x2D043F00); // r_b_addr
        wr(0x40000408, 0x2D044700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 160: /text_model/encoder/layers.3/mlp/fc1/Add (Opcode 0x15)
        wr(0x40000444, 0x0015A00); // r_a_addr
        wr(0x40000448, 0x2D044700); // r_b_addr
        wr(0x40000408, 0x2D0A8700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 2048); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 161: /text_model/encoder/layers.3/mlp/activation_fn/Mul (Opcode 0x15)
        wr(0x40000444, 0x2D0A8700); // r_a_addr
        wr(0x40000448, 0x2D10C700); // r_b_addr
        wr(0x40000408, 0x2D10CB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 162: /text_model/encoder/layers.3/mlp/activation_fn/Sigmoid (Opcode 0x15)
        wr(0x40000444, 0x2D10CB00); // r_a_addr
        wr(0x40000448, 0x2D10CB00); // r_b_addr
        wr(0x40000408, 0x2D170B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 163: /text_model/encoder/layers.3/mlp/activation_fn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x2D0A8700); // r_a_addr
        wr(0x40000448, 0x2D170B00); // r_b_addr
        wr(0x40000408, 0x2D1D4B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 164: /text_model/encoder/layers.3/mlp/fc2/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x2D238B00); // r_a_addr
        wr(0x40000448, 0x2356600); // r_b_addr
        wr(0x40000408, 0x2D238F00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 165: /text_model/encoder/layers.3/mlp/fc2/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x2D1D4B00); // r_a_addr
        wr(0x40000448, 0x2D238F00); // r_b_addr
        wr(0x40000408, 0x2D239300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 166: /text_model/encoder/layers.3/mlp/fc2/Add (Opcode 0x15)
        wr(0x40000444, 0x0016200); // r_a_addr
        wr(0x40000448, 0x2D239300); // r_b_addr
        wr(0x40000408, 0x2D29D300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 167: /text_model/encoder/layers.3/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x2CDEB700); // r_a_addr
        wr(0x40000448, 0x2D29D300); // r_b_addr
        wr(0x40000408, 0x2D301300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 168: /text_model/encoder/layers.4/layer_norm1/Sub (Opcode 0x15)
        wr(0x40000444, 0x2D301300); // r_a_addr
        wr(0x40000448, 0x2D301300); // r_b_addr
        wr(0x40000408, 0x2D365300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 169: /text_model/encoder/layers.4/layer_norm1/Add (Opcode 0x15)
        wr(0x40000444, 0x2D365300); // r_a_addr
        wr(0x40000448, 0x2D3C9300); // r_b_addr
        wr(0x40000408, 0x2D3C9700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 170: /text_model/encoder/layers.4/layer_norm1/Div (Opcode 0x15)
        wr(0x40000444, 0x2D365300); // r_a_addr
        wr(0x40000448, 0x2D3C9700); // r_b_addr
        wr(0x40000408, 0x2D42D700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 171: /text_model/encoder/layers.4/layer_norm1/Mul (Opcode 0x15)
        wr(0x40000444, 0x2D42D700); // r_a_addr
        wr(0x40000448, 0x0017000); // r_b_addr
        wr(0x40000408, 0x2D491700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 172: /text_model/encoder/layers.4/layer_norm1/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x2D491700); // r_a_addr
        wr(0x40000448, 0x0017200); // r_b_addr
        wr(0x40000408, 0x2D4F5700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 173: /text_model/encoder/layers.4/self_attn/q_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x2D559700); // r_a_addr
        wr(0x40000448, 0x2456A00); // r_b_addr
        wr(0x40000408, 0x2D559B00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 174: /text_model/encoder/layers.4/self_attn/k_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x2D559700); // r_a_addr
        wr(0x40000448, 0x2496E00); // r_b_addr
        wr(0x40000408, 0x2D559F00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 175: /text_model/encoder/layers.4/self_attn/v_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x2D559700); // r_a_addr
        wr(0x40000448, 0x24D7200); // r_b_addr
        wr(0x40000408, 0x2D55A300); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 176: /text_model/encoder/layers.4/self_attn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x2D4F5700); // r_a_addr
        wr(0x40000448, 0x2D55A700); // r_b_addr
        wr(0x40000408, 0x2D55AB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 177: /text_model/encoder/layers.4/self_attn/q_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x2D4F5700); // r_a_addr
        wr(0x40000448, 0x2D559B00); // r_b_addr
        wr(0x40000408, 0x2D94D500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 178: /text_model/encoder/layers.4/self_attn/k_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x2D4F5700); // r_a_addr
        wr(0x40000448, 0x2D559F00); // r_b_addr
        wr(0x40000408, 0x2D9B1500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 179: /text_model/encoder/layers.4/self_attn/v_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x2D4F5700); // r_a_addr
        wr(0x40000448, 0x2D55A300); // r_b_addr
        wr(0x40000408, 0x2DA15500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 180: /text_model/encoder/layers.4/self_attn/q_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0016C00); // r_a_addr
        wr(0x40000448, 0x2D94D500); // r_b_addr
        wr(0x40000408, 0x2DBABB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 181: /text_model/encoder/layers.4/self_attn/k_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0016800); // r_a_addr
        wr(0x40000448, 0x2D9B1500); // r_b_addr
        wr(0x40000408, 0x2DC0FB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 182: /text_model/encoder/layers.4/self_attn/v_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0016A00); // r_a_addr
        wr(0x40000448, 0x2DA15500); // r_b_addr
        wr(0x40000408, 0x2DC73B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 183: /text_model/encoder/layers.4/self_attn/Mul (Opcode 0x15)
        wr(0x40000444, 0x2DBABB00); // r_a_addr
        wr(0x40000448, 0x2DCD7B00); // r_b_addr
        wr(0x40000408, 0x2DCD7F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 184: /text_model/encoder/layers.4/self_attn/MatMul (Opcode 0x12)
        wr(0x40000400, 0x2DCD7F00); // r_in_addr
        wr(0x40000404, 0x2DC0FB00); // r_w_addr
        wr(0x40000408, 0x2DD3BF00); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 185: /text_model/encoder/layers.4/self_attn/Add (Opcode 0x15)
        wr(0x40000444, 0x2DD3BF00); // r_a_addr
        wr(0x40000448, 0x3CB1E00); // r_b_addr
        wr(0x40000408, 0x321EE100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 186: /text_model/encoder/layers.4/self_attn/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x321EE100); // r_a_addr
        wr(0x40000448, 0x3D18700); // r_b_addr
        wr(0x40000408, 0x32252100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 187: /text_model/encoder/layers.4/self_attn/Softmax (Opcode 0x13)
        wr(0x40000444, 0x32252100); // r_q_addr
        wr(0x40000448, 0x32252100); // r_k_addr
        wr(0x4000044C, 0x32252100); // r_v_addr
        wr(0x40000408, 0x322B6100); // r_out_addr
        wr(0x40000450, 4096); // r_seq_len
        wr(0x40000454, 1); // r_num_heads
        wr(0x40000458, 4096); // r_head_dim
        wr(0x40000310, 0x13);

        // Instruction 188: /text_model/encoder/layers.4/self_attn/MatMul_1 (Opcode 0x12)
        wr(0x40000400, 0x322B6100); // r_in_addr
        wr(0x40000404, 0x2DC73B00); // r_w_addr
        wr(0x40000408, 0x332B6100); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 189: /text_model/encoder/layers.4/self_attn/out_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x372B6100); // r_a_addr
        wr(0x40000448, 0x2517600); // r_b_addr
        wr(0x40000408, 0x372B6500); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 190: /text_model/encoder/layers.4/self_attn/out_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x332B6100); // r_a_addr
        wr(0x40000448, 0x372B6500); // r_b_addr
        wr(0x40000408, 0x372B6900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 191: /text_model/encoder/layers.4/self_attn/out_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0016E00); // r_a_addr
        wr(0x40000448, 0x372B6900); // r_b_addr
        wr(0x40000408, 0x3731A900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 192: /text_model/encoder/layers.4/Add (Opcode 0x15)
        wr(0x40000444, 0x2D301300); // r_a_addr
        wr(0x40000448, 0x3731A900); // r_b_addr
        wr(0x40000408, 0x3737E900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 193: /text_model/encoder/layers.4/layer_norm2/Sub (Opcode 0x15)
        wr(0x40000444, 0x3737E900); // r_a_addr
        wr(0x40000448, 0x3737E900); // r_b_addr
        wr(0x40000408, 0x373E2900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 194: /text_model/encoder/layers.4/layer_norm2/Add (Opcode 0x15)
        wr(0x40000444, 0x373E2900); // r_a_addr
        wr(0x40000448, 0x37446900); // r_b_addr
        wr(0x40000408, 0x37446D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 195: /text_model/encoder/layers.4/layer_norm2/Div (Opcode 0x15)
        wr(0x40000444, 0x373E2900); // r_a_addr
        wr(0x40000448, 0x37446D00); // r_b_addr
        wr(0x40000408, 0x374AAD00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 196: /text_model/encoder/layers.4/layer_norm2/Mul (Opcode 0x15)
        wr(0x40000444, 0x374AAD00); // r_a_addr
        wr(0x40000448, 0x0017E00); // r_b_addr
        wr(0x40000408, 0x3750ED00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 197: /text_model/encoder/layers.4/layer_norm2/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x3750ED00); // r_a_addr
        wr(0x40000448, 0x0018000); // r_b_addr
        wr(0x40000408, 0x37572D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 198: /text_model/encoder/layers.4/mlp/fc1/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x375D6D00); // r_a_addr
        wr(0x40000448, 0x2557A00); // r_b_addr
        wr(0x40000408, 0x375D7100); // r_out_addr
        wr(0x40000450, 2048); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 199: /text_model/encoder/layers.4/mlp/fc1/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x37572D00); // r_a_addr
        wr(0x40000448, 0x375D7100); // r_b_addr
        wr(0x40000408, 0x375D7900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 200: /text_model/encoder/layers.4/mlp/fc1/Add (Opcode 0x15)
        wr(0x40000444, 0x0017400); // r_a_addr
        wr(0x40000448, 0x375D7900); // r_b_addr
        wr(0x40000408, 0x3763B900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 2048); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 201: /text_model/encoder/layers.4/mlp/activation_fn/Mul (Opcode 0x15)
        wr(0x40000444, 0x3763B900); // r_a_addr
        wr(0x40000448, 0x3769F900); // r_b_addr
        wr(0x40000408, 0x3769FD00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 202: /text_model/encoder/layers.4/mlp/activation_fn/Sigmoid (Opcode 0x15)
        wr(0x40000444, 0x3769FD00); // r_a_addr
        wr(0x40000448, 0x3769FD00); // r_b_addr
        wr(0x40000408, 0x37703D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 203: /text_model/encoder/layers.4/mlp/activation_fn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x3763B900); // r_a_addr
        wr(0x40000448, 0x37703D00); // r_b_addr
        wr(0x40000408, 0x37767D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 204: /text_model/encoder/layers.4/mlp/fc2/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x377CBD00); // r_a_addr
        wr(0x40000448, 0x2658A00); // r_b_addr
        wr(0x40000408, 0x377CC100); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 205: /text_model/encoder/layers.4/mlp/fc2/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x37767D00); // r_a_addr
        wr(0x40000448, 0x377CC100); // r_b_addr
        wr(0x40000408, 0x377CC500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 206: /text_model/encoder/layers.4/mlp/fc2/Add (Opcode 0x15)
        wr(0x40000444, 0x0017C00); // r_a_addr
        wr(0x40000448, 0x377CC500); // r_b_addr
        wr(0x40000408, 0x37830500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 207: /text_model/encoder/layers.4/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x3737E900); // r_a_addr
        wr(0x40000448, 0x37830500); // r_b_addr
        wr(0x40000408, 0x37894500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 208: /text_model/encoder/layers.5/layer_norm1/Sub (Opcode 0x15)
        wr(0x40000444, 0x37894500); // r_a_addr
        wr(0x40000448, 0x37894500); // r_b_addr
        wr(0x40000408, 0x378F8500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 209: /text_model/encoder/layers.5/layer_norm1/Add (Opcode 0x15)
        wr(0x40000444, 0x378F8500); // r_a_addr
        wr(0x40000448, 0x3795C500); // r_b_addr
        wr(0x40000408, 0x3795C900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 210: /text_model/encoder/layers.5/layer_norm1/Div (Opcode 0x15)
        wr(0x40000444, 0x378F8500); // r_a_addr
        wr(0x40000448, 0x3795C900); // r_b_addr
        wr(0x40000408, 0x379C0900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 211: /text_model/encoder/layers.5/layer_norm1/Mul (Opcode 0x15)
        wr(0x40000444, 0x379C0900); // r_a_addr
        wr(0x40000448, 0x0018A00); // r_b_addr
        wr(0x40000408, 0x37A24900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 212: /text_model/encoder/layers.5/layer_norm1/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x37A24900); // r_a_addr
        wr(0x40000448, 0x0018C00); // r_b_addr
        wr(0x40000408, 0x37A88900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 213: /text_model/encoder/layers.5/self_attn/q_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x37AEC900); // r_a_addr
        wr(0x40000448, 0x2758E00); // r_b_addr
        wr(0x40000408, 0x37AECD00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 214: /text_model/encoder/layers.5/self_attn/k_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x37AEC900); // r_a_addr
        wr(0x40000448, 0x2799200); // r_b_addr
        wr(0x40000408, 0x37AED100); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 215: /text_model/encoder/layers.5/self_attn/v_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x37AEC900); // r_a_addr
        wr(0x40000448, 0x27D9600); // r_b_addr
        wr(0x40000408, 0x37AED500); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 216: /text_model/encoder/layers.5/self_attn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x37A88900); // r_a_addr
        wr(0x40000448, 0x37AED900); // r_b_addr
        wr(0x40000408, 0x37AEDD00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 217: /text_model/encoder/layers.5/self_attn/q_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x37A88900); // r_a_addr
        wr(0x40000448, 0x37AECD00); // r_b_addr
        wr(0x40000408, 0x37EE0700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 218: /text_model/encoder/layers.5/self_attn/k_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x37A88900); // r_a_addr
        wr(0x40000448, 0x37AED100); // r_b_addr
        wr(0x40000408, 0x37F44700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 219: /text_model/encoder/layers.5/self_attn/v_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x37A88900); // r_a_addr
        wr(0x40000448, 0x37AED500); // r_b_addr
        wr(0x40000408, 0x37FA8700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 220: /text_model/encoder/layers.5/self_attn/q_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0018600); // r_a_addr
        wr(0x40000448, 0x37EE0700); // r_b_addr
        wr(0x40000408, 0x3813ED00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 221: /text_model/encoder/layers.5/self_attn/k_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0018200); // r_a_addr
        wr(0x40000448, 0x37F44700); // r_b_addr
        wr(0x40000408, 0x381A2D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 222: /text_model/encoder/layers.5/self_attn/v_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0018400); // r_a_addr
        wr(0x40000448, 0x37FA8700); // r_b_addr
        wr(0x40000408, 0x38206D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 223: /text_model/encoder/layers.5/self_attn/Mul (Opcode 0x15)
        wr(0x40000444, 0x3813ED00); // r_a_addr
        wr(0x40000448, 0x3826AD00); // r_b_addr
        wr(0x40000408, 0x3826B100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 224: /text_model/encoder/layers.5/self_attn/MatMul (Opcode 0x12)
        wr(0x40000400, 0x3826B100); // r_in_addr
        wr(0x40000404, 0x381A2D00); // r_w_addr
        wr(0x40000408, 0x382CF100); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 225: /text_model/encoder/layers.5/self_attn/Add (Opcode 0x15)
        wr(0x40000444, 0x382CF100); // r_a_addr
        wr(0x40000448, 0x3CB1E00); // r_b_addr
        wr(0x40000408, 0x3C781300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 226: /text_model/encoder/layers.5/self_attn/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x3C781300); // r_a_addr
        wr(0x40000448, 0x3D18700); // r_b_addr
        wr(0x40000408, 0x3C7E5300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 227: /text_model/encoder/layers.5/self_attn/Softmax (Opcode 0x13)
        wr(0x40000444, 0x3C7E5300); // r_q_addr
        wr(0x40000448, 0x3C7E5300); // r_k_addr
        wr(0x4000044C, 0x3C7E5300); // r_v_addr
        wr(0x40000408, 0x3C849300); // r_out_addr
        wr(0x40000450, 4096); // r_seq_len
        wr(0x40000454, 1); // r_num_heads
        wr(0x40000458, 4096); // r_head_dim
        wr(0x40000310, 0x13);

        // Instruction 228: /text_model/encoder/layers.5/self_attn/MatMul_1 (Opcode 0x12)
        wr(0x40000400, 0x3C849300); // r_in_addr
        wr(0x40000404, 0x38206D00); // r_w_addr
        wr(0x40000408, 0x3D849300); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 229: /text_model/encoder/layers.5/self_attn/out_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x41849300); // r_a_addr
        wr(0x40000448, 0x2819A00); // r_b_addr
        wr(0x40000408, 0x41849700); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 230: /text_model/encoder/layers.5/self_attn/out_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x3D849300); // r_a_addr
        wr(0x40000448, 0x41849700); // r_b_addr
        wr(0x40000408, 0x41849B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 231: /text_model/encoder/layers.5/self_attn/out_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0018800); // r_a_addr
        wr(0x40000448, 0x41849B00); // r_b_addr
        wr(0x40000408, 0x418ADB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 232: /text_model/encoder/layers.5/Add (Opcode 0x15)
        wr(0x40000444, 0x37894500); // r_a_addr
        wr(0x40000448, 0x418ADB00); // r_b_addr
        wr(0x40000408, 0x41911B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 233: /text_model/encoder/layers.5/layer_norm2/Sub (Opcode 0x15)
        wr(0x40000444, 0x41911B00); // r_a_addr
        wr(0x40000448, 0x41911B00); // r_b_addr
        wr(0x40000408, 0x41975B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 234: /text_model/encoder/layers.5/layer_norm2/Add (Opcode 0x15)
        wr(0x40000444, 0x41975B00); // r_a_addr
        wr(0x40000448, 0x419D9B00); // r_b_addr
        wr(0x40000408, 0x419D9F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 235: /text_model/encoder/layers.5/layer_norm2/Div (Opcode 0x15)
        wr(0x40000444, 0x41975B00); // r_a_addr
        wr(0x40000448, 0x419D9F00); // r_b_addr
        wr(0x40000408, 0x41A3DF00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 236: /text_model/encoder/layers.5/layer_norm2/Mul (Opcode 0x15)
        wr(0x40000444, 0x41A3DF00); // r_a_addr
        wr(0x40000448, 0x0019800); // r_b_addr
        wr(0x40000408, 0x41AA1F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 237: /text_model/encoder/layers.5/layer_norm2/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x41AA1F00); // r_a_addr
        wr(0x40000448, 0x0019A00); // r_b_addr
        wr(0x40000408, 0x41B05F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 238: /text_model/encoder/layers.5/mlp/fc1/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x41B69F00); // r_a_addr
        wr(0x40000448, 0x2859E00); // r_b_addr
        wr(0x40000408, 0x41B6A300); // r_out_addr
        wr(0x40000450, 2048); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 239: /text_model/encoder/layers.5/mlp/fc1/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x41B05F00); // r_a_addr
        wr(0x40000448, 0x41B6A300); // r_b_addr
        wr(0x40000408, 0x41B6AB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 240: /text_model/encoder/layers.5/mlp/fc1/Add (Opcode 0x15)
        wr(0x40000444, 0x0018E00); // r_a_addr
        wr(0x40000448, 0x41B6AB00); // r_b_addr
        wr(0x40000408, 0x41BCEB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 2048); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 241: /text_model/encoder/layers.5/mlp/activation_fn/Mul (Opcode 0x15)
        wr(0x40000444, 0x41BCEB00); // r_a_addr
        wr(0x40000448, 0x41C32B00); // r_b_addr
        wr(0x40000408, 0x41C32F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 242: /text_model/encoder/layers.5/mlp/activation_fn/Sigmoid (Opcode 0x15)
        wr(0x40000444, 0x41C32F00); // r_a_addr
        wr(0x40000448, 0x41C32F00); // r_b_addr
        wr(0x40000408, 0x41C96F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 243: /text_model/encoder/layers.5/mlp/activation_fn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x41BCEB00); // r_a_addr
        wr(0x40000448, 0x41C96F00); // r_b_addr
        wr(0x40000408, 0x41CFAF00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 244: /text_model/encoder/layers.5/mlp/fc2/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x41D5EF00); // r_a_addr
        wr(0x40000448, 0x295AE00); // r_b_addr
        wr(0x40000408, 0x41D5F300); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 245: /text_model/encoder/layers.5/mlp/fc2/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x41CFAF00); // r_a_addr
        wr(0x40000448, 0x41D5F300); // r_b_addr
        wr(0x40000408, 0x41D5F700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 246: /text_model/encoder/layers.5/mlp/fc2/Add (Opcode 0x15)
        wr(0x40000444, 0x0019600); // r_a_addr
        wr(0x40000448, 0x41D5F700); // r_b_addr
        wr(0x40000408, 0x41DC3700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 247: /text_model/encoder/layers.5/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x41911B00); // r_a_addr
        wr(0x40000448, 0x41DC3700); // r_b_addr
        wr(0x40000408, 0x41E27700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 248: /text_model/encoder/layers.6/layer_norm1/Sub (Opcode 0x15)
        wr(0x40000444, 0x41E27700); // r_a_addr
        wr(0x40000448, 0x41E27700); // r_b_addr
        wr(0x40000408, 0x41E8B700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 249: /text_model/encoder/layers.6/layer_norm1/Add (Opcode 0x15)
        wr(0x40000444, 0x41E8B700); // r_a_addr
        wr(0x40000448, 0x41EEF700); // r_b_addr
        wr(0x40000408, 0x41EEFB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 250: /text_model/encoder/layers.6/layer_norm1/Div (Opcode 0x15)
        wr(0x40000444, 0x41E8B700); // r_a_addr
        wr(0x40000448, 0x41EEFB00); // r_b_addr
        wr(0x40000408, 0x41F53B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 251: /text_model/encoder/layers.6/layer_norm1/Mul (Opcode 0x15)
        wr(0x40000444, 0x41F53B00); // r_a_addr
        wr(0x40000448, 0x001A400); // r_b_addr
        wr(0x40000408, 0x41FB7B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 252: /text_model/encoder/layers.6/layer_norm1/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x41FB7B00); // r_a_addr
        wr(0x40000448, 0x001A600); // r_b_addr
        wr(0x40000408, 0x4201BB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 253: /text_model/encoder/layers.6/self_attn/q_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x4207FB00); // r_a_addr
        wr(0x40000448, 0x2A5B200); // r_b_addr
        wr(0x40000408, 0x4207FF00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 254: /text_model/encoder/layers.6/self_attn/k_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x4207FB00); // r_a_addr
        wr(0x40000448, 0x2A9B600); // r_b_addr
        wr(0x40000408, 0x42080300); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 255: /text_model/encoder/layers.6/self_attn/v_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x4207FB00); // r_a_addr
        wr(0x40000448, 0x2ADBA00); // r_b_addr
        wr(0x40000408, 0x42080700); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 256: /text_model/encoder/layers.6/self_attn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x4201BB00); // r_a_addr
        wr(0x40000448, 0x42080B00); // r_b_addr
        wr(0x40000408, 0x42080F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 257: /text_model/encoder/layers.6/self_attn/q_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x4201BB00); // r_a_addr
        wr(0x40000448, 0x4207FF00); // r_b_addr
        wr(0x40000408, 0x42473900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 258: /text_model/encoder/layers.6/self_attn/k_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x4201BB00); // r_a_addr
        wr(0x40000448, 0x42080300); // r_b_addr
        wr(0x40000408, 0x424D7900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 259: /text_model/encoder/layers.6/self_attn/v_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x4201BB00); // r_a_addr
        wr(0x40000448, 0x42080700); // r_b_addr
        wr(0x40000408, 0x4253B900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 260: /text_model/encoder/layers.6/self_attn/q_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x001A000); // r_a_addr
        wr(0x40000448, 0x42473900); // r_b_addr
        wr(0x40000408, 0x426D1F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 261: /text_model/encoder/layers.6/self_attn/k_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0019C00); // r_a_addr
        wr(0x40000448, 0x424D7900); // r_b_addr
        wr(0x40000408, 0x42735F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 262: /text_model/encoder/layers.6/self_attn/v_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0019E00); // r_a_addr
        wr(0x40000448, 0x4253B900); // r_b_addr
        wr(0x40000408, 0x42799F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 263: /text_model/encoder/layers.6/self_attn/Mul (Opcode 0x15)
        wr(0x40000444, 0x426D1F00); // r_a_addr
        wr(0x40000448, 0x427FDF00); // r_b_addr
        wr(0x40000408, 0x427FE300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 264: /text_model/encoder/layers.6/self_attn/MatMul (Opcode 0x12)
        wr(0x40000400, 0x427FE300); // r_in_addr
        wr(0x40000404, 0x42735F00); // r_w_addr
        wr(0x40000408, 0x42862300); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 265: /text_model/encoder/layers.6/self_attn/Add (Opcode 0x15)
        wr(0x40000444, 0x42862300); // r_a_addr
        wr(0x40000448, 0x3CB1E00); // r_b_addr
        wr(0x40000408, 0x46D14500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 266: /text_model/encoder/layers.6/self_attn/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x46D14500); // r_a_addr
        wr(0x40000448, 0x3D18700); // r_b_addr
        wr(0x40000408, 0x46D78500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 267: /text_model/encoder/layers.6/self_attn/Softmax (Opcode 0x13)
        wr(0x40000444, 0x46D78500); // r_q_addr
        wr(0x40000448, 0x46D78500); // r_k_addr
        wr(0x4000044C, 0x46D78500); // r_v_addr
        wr(0x40000408, 0x46DDC500); // r_out_addr
        wr(0x40000450, 4096); // r_seq_len
        wr(0x40000454, 1); // r_num_heads
        wr(0x40000458, 4096); // r_head_dim
        wr(0x40000310, 0x13);

        // Instruction 268: /text_model/encoder/layers.6/self_attn/MatMul_1 (Opcode 0x12)
        wr(0x40000400, 0x46DDC500); // r_in_addr
        wr(0x40000404, 0x42799F00); // r_w_addr
        wr(0x40000408, 0x47DDC500); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 269: /text_model/encoder/layers.6/self_attn/out_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x4BDDC500); // r_a_addr
        wr(0x40000448, 0x2B1BE00); // r_b_addr
        wr(0x40000408, 0x4BDDC900); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 270: /text_model/encoder/layers.6/self_attn/out_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x47DDC500); // r_a_addr
        wr(0x40000448, 0x4BDDC900); // r_b_addr
        wr(0x40000408, 0x4BDDCD00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 271: /text_model/encoder/layers.6/self_attn/out_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x001A200); // r_a_addr
        wr(0x40000448, 0x4BDDCD00); // r_b_addr
        wr(0x40000408, 0x4BE40D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 272: /text_model/encoder/layers.6/Add (Opcode 0x15)
        wr(0x40000444, 0x41E27700); // r_a_addr
        wr(0x40000448, 0x4BE40D00); // r_b_addr
        wr(0x40000408, 0x4BEA4D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 273: /text_model/encoder/layers.6/layer_norm2/Sub (Opcode 0x15)
        wr(0x40000444, 0x4BEA4D00); // r_a_addr
        wr(0x40000448, 0x4BEA4D00); // r_b_addr
        wr(0x40000408, 0x4BF08D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 274: /text_model/encoder/layers.6/layer_norm2/Add (Opcode 0x15)
        wr(0x40000444, 0x4BF08D00); // r_a_addr
        wr(0x40000448, 0x4BF6CD00); // r_b_addr
        wr(0x40000408, 0x4BF6D100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 275: /text_model/encoder/layers.6/layer_norm2/Div (Opcode 0x15)
        wr(0x40000444, 0x4BF08D00); // r_a_addr
        wr(0x40000448, 0x4BF6D100); // r_b_addr
        wr(0x40000408, 0x4BFD1100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 276: /text_model/encoder/layers.6/layer_norm2/Mul (Opcode 0x15)
        wr(0x40000444, 0x4BFD1100); // r_a_addr
        wr(0x40000448, 0x001B200); // r_b_addr
        wr(0x40000408, 0x4C035100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 277: /text_model/encoder/layers.6/layer_norm2/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x4C035100); // r_a_addr
        wr(0x40000448, 0x001B400); // r_b_addr
        wr(0x40000408, 0x4C099100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 278: /text_model/encoder/layers.6/mlp/fc1/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x4C0FD100); // r_a_addr
        wr(0x40000448, 0x2B5C200); // r_b_addr
        wr(0x40000408, 0x4C0FD500); // r_out_addr
        wr(0x40000450, 2048); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 279: /text_model/encoder/layers.6/mlp/fc1/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x4C099100); // r_a_addr
        wr(0x40000448, 0x4C0FD500); // r_b_addr
        wr(0x40000408, 0x4C0FDD00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 280: /text_model/encoder/layers.6/mlp/fc1/Add (Opcode 0x15)
        wr(0x40000444, 0x001A800); // r_a_addr
        wr(0x40000448, 0x4C0FDD00); // r_b_addr
        wr(0x40000408, 0x4C161D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 2048); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 281: /text_model/encoder/layers.6/mlp/activation_fn/Mul (Opcode 0x15)
        wr(0x40000444, 0x4C161D00); // r_a_addr
        wr(0x40000448, 0x4C1C5D00); // r_b_addr
        wr(0x40000408, 0x4C1C6100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 282: /text_model/encoder/layers.6/mlp/activation_fn/Sigmoid (Opcode 0x15)
        wr(0x40000444, 0x4C1C6100); // r_a_addr
        wr(0x40000448, 0x4C1C6100); // r_b_addr
        wr(0x40000408, 0x4C22A100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 283: /text_model/encoder/layers.6/mlp/activation_fn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x4C161D00); // r_a_addr
        wr(0x40000448, 0x4C22A100); // r_b_addr
        wr(0x40000408, 0x4C28E100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 284: /text_model/encoder/layers.6/mlp/fc2/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x4C2F2100); // r_a_addr
        wr(0x40000448, 0x2C5D200); // r_b_addr
        wr(0x40000408, 0x4C2F2500); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 285: /text_model/encoder/layers.6/mlp/fc2/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x4C28E100); // r_a_addr
        wr(0x40000448, 0x4C2F2500); // r_b_addr
        wr(0x40000408, 0x4C2F2900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 286: /text_model/encoder/layers.6/mlp/fc2/Add (Opcode 0x15)
        wr(0x40000444, 0x001B000); // r_a_addr
        wr(0x40000448, 0x4C2F2900); // r_b_addr
        wr(0x40000408, 0x4C356900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 287: /text_model/encoder/layers.6/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x4BEA4D00); // r_a_addr
        wr(0x40000448, 0x4C356900); // r_b_addr
        wr(0x40000408, 0x4C3BA900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 288: /text_model/encoder/layers.7/layer_norm1/Sub (Opcode 0x15)
        wr(0x40000444, 0x4C3BA900); // r_a_addr
        wr(0x40000448, 0x4C3BA900); // r_b_addr
        wr(0x40000408, 0x4C41E900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 289: /text_model/encoder/layers.7/layer_norm1/Add (Opcode 0x15)
        wr(0x40000444, 0x4C41E900); // r_a_addr
        wr(0x40000448, 0x4C482900); // r_b_addr
        wr(0x40000408, 0x4C482D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 290: /text_model/encoder/layers.7/layer_norm1/Div (Opcode 0x15)
        wr(0x40000444, 0x4C41E900); // r_a_addr
        wr(0x40000448, 0x4C482D00); // r_b_addr
        wr(0x40000408, 0x4C4E6D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 291: /text_model/encoder/layers.7/layer_norm1/Mul (Opcode 0x15)
        wr(0x40000444, 0x4C4E6D00); // r_a_addr
        wr(0x40000448, 0x001BE00); // r_b_addr
        wr(0x40000408, 0x4C54AD00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 292: /text_model/encoder/layers.7/layer_norm1/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x4C54AD00); // r_a_addr
        wr(0x40000448, 0x001C000); // r_b_addr
        wr(0x40000408, 0x4C5AED00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 293: /text_model/encoder/layers.7/self_attn/q_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x4C612D00); // r_a_addr
        wr(0x40000448, 0x2D5D600); // r_b_addr
        wr(0x40000408, 0x4C613100); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 294: /text_model/encoder/layers.7/self_attn/k_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x4C612D00); // r_a_addr
        wr(0x40000448, 0x2D9DA00); // r_b_addr
        wr(0x40000408, 0x4C613500); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 295: /text_model/encoder/layers.7/self_attn/v_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x4C612D00); // r_a_addr
        wr(0x40000448, 0x2DDDE00); // r_b_addr
        wr(0x40000408, 0x4C613900); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 296: /text_model/encoder/layers.7/self_attn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x4C5AED00); // r_a_addr
        wr(0x40000448, 0x4C613D00); // r_b_addr
        wr(0x40000408, 0x4C614100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 297: /text_model/encoder/layers.7/self_attn/q_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x4C5AED00); // r_a_addr
        wr(0x40000448, 0x4C613100); // r_b_addr
        wr(0x40000408, 0x4CA06B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 298: /text_model/encoder/layers.7/self_attn/k_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x4C5AED00); // r_a_addr
        wr(0x40000448, 0x4C613500); // r_b_addr
        wr(0x40000408, 0x4CA6AB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 299: /text_model/encoder/layers.7/self_attn/v_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x4C5AED00); // r_a_addr
        wr(0x40000448, 0x4C613900); // r_b_addr
        wr(0x40000408, 0x4CACEB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 300: /text_model/encoder/layers.7/self_attn/q_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x001BA00); // r_a_addr
        wr(0x40000448, 0x4CA06B00); // r_b_addr
        wr(0x40000408, 0x4CC65100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 301: /text_model/encoder/layers.7/self_attn/k_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x001B600); // r_a_addr
        wr(0x40000448, 0x4CA6AB00); // r_b_addr
        wr(0x40000408, 0x4CCC9100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 302: /text_model/encoder/layers.7/self_attn/v_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x001B800); // r_a_addr
        wr(0x40000448, 0x4CACEB00); // r_b_addr
        wr(0x40000408, 0x4CD2D100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 303: /text_model/encoder/layers.7/self_attn/Mul (Opcode 0x15)
        wr(0x40000444, 0x4CC65100); // r_a_addr
        wr(0x40000448, 0x4CD91100); // r_b_addr
        wr(0x40000408, 0x4CD91500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 304: /text_model/encoder/layers.7/self_attn/MatMul (Opcode 0x12)
        wr(0x40000400, 0x4CD91500); // r_in_addr
        wr(0x40000404, 0x4CCC9100); // r_w_addr
        wr(0x40000408, 0x4CDF5500); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 305: /text_model/encoder/layers.7/self_attn/Add (Opcode 0x15)
        wr(0x40000444, 0x4CDF5500); // r_a_addr
        wr(0x40000448, 0x3CB1E00); // r_b_addr
        wr(0x40000408, 0x512A7700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 306: /text_model/encoder/layers.7/self_attn/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x512A7700); // r_a_addr
        wr(0x40000448, 0x3D18700); // r_b_addr
        wr(0x40000408, 0x5130B700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 307: /text_model/encoder/layers.7/self_attn/Softmax (Opcode 0x13)
        wr(0x40000444, 0x5130B700); // r_q_addr
        wr(0x40000448, 0x5130B700); // r_k_addr
        wr(0x4000044C, 0x5130B700); // r_v_addr
        wr(0x40000408, 0x5136F700); // r_out_addr
        wr(0x40000450, 4096); // r_seq_len
        wr(0x40000454, 1); // r_num_heads
        wr(0x40000458, 4096); // r_head_dim
        wr(0x40000310, 0x13);

        // Instruction 308: /text_model/encoder/layers.7/self_attn/MatMul_1 (Opcode 0x12)
        wr(0x40000400, 0x5136F700); // r_in_addr
        wr(0x40000404, 0x4CD2D100); // r_w_addr
        wr(0x40000408, 0x5236F700); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 309: /text_model/encoder/layers.7/self_attn/out_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x5636F700); // r_a_addr
        wr(0x40000448, 0x2E1E200); // r_b_addr
        wr(0x40000408, 0x5636FB00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 310: /text_model/encoder/layers.7/self_attn/out_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x5236F700); // r_a_addr
        wr(0x40000448, 0x5636FB00); // r_b_addr
        wr(0x40000408, 0x5636FF00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 311: /text_model/encoder/layers.7/self_attn/out_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x001BC00); // r_a_addr
        wr(0x40000448, 0x5636FF00); // r_b_addr
        wr(0x40000408, 0x563D3F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 312: /text_model/encoder/layers.7/Add (Opcode 0x15)
        wr(0x40000444, 0x4C3BA900); // r_a_addr
        wr(0x40000448, 0x563D3F00); // r_b_addr
        wr(0x40000408, 0x56437F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 313: /text_model/encoder/layers.7/layer_norm2/Sub (Opcode 0x15)
        wr(0x40000444, 0x56437F00); // r_a_addr
        wr(0x40000448, 0x56437F00); // r_b_addr
        wr(0x40000408, 0x5649BF00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 314: /text_model/encoder/layers.7/layer_norm2/Add (Opcode 0x15)
        wr(0x40000444, 0x5649BF00); // r_a_addr
        wr(0x40000448, 0x564FFF00); // r_b_addr
        wr(0x40000408, 0x56500300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 315: /text_model/encoder/layers.7/layer_norm2/Div (Opcode 0x15)
        wr(0x40000444, 0x5649BF00); // r_a_addr
        wr(0x40000448, 0x56500300); // r_b_addr
        wr(0x40000408, 0x56564300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 316: /text_model/encoder/layers.7/layer_norm2/Mul (Opcode 0x15)
        wr(0x40000444, 0x56564300); // r_a_addr
        wr(0x40000448, 0x001CC00); // r_b_addr
        wr(0x40000408, 0x565C8300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 317: /text_model/encoder/layers.7/layer_norm2/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x565C8300); // r_a_addr
        wr(0x40000448, 0x001CE00); // r_b_addr
        wr(0x40000408, 0x5662C300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 318: /text_model/encoder/layers.7/mlp/fc1/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x56690300); // r_a_addr
        wr(0x40000448, 0x2E5E600); // r_b_addr
        wr(0x40000408, 0x56690700); // r_out_addr
        wr(0x40000450, 2048); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 319: /text_model/encoder/layers.7/mlp/fc1/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x5662C300); // r_a_addr
        wr(0x40000448, 0x56690700); // r_b_addr
        wr(0x40000408, 0x56690F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 320: /text_model/encoder/layers.7/mlp/fc1/Add (Opcode 0x15)
        wr(0x40000444, 0x001C200); // r_a_addr
        wr(0x40000448, 0x56690F00); // r_b_addr
        wr(0x40000408, 0x566F4F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 2048); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 321: /text_model/encoder/layers.7/mlp/activation_fn/Mul (Opcode 0x15)
        wr(0x40000444, 0x566F4F00); // r_a_addr
        wr(0x40000448, 0x56758F00); // r_b_addr
        wr(0x40000408, 0x56759300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 322: /text_model/encoder/layers.7/mlp/activation_fn/Sigmoid (Opcode 0x15)
        wr(0x40000444, 0x56759300); // r_a_addr
        wr(0x40000448, 0x56759300); // r_b_addr
        wr(0x40000408, 0x567BD300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 323: /text_model/encoder/layers.7/mlp/activation_fn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x566F4F00); // r_a_addr
        wr(0x40000448, 0x567BD300); // r_b_addr
        wr(0x40000408, 0x56821300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 324: /text_model/encoder/layers.7/mlp/fc2/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x56885300); // r_a_addr
        wr(0x40000448, 0x2F5F600); // r_b_addr
        wr(0x40000408, 0x56885700); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 325: /text_model/encoder/layers.7/mlp/fc2/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x56821300); // r_a_addr
        wr(0x40000448, 0x56885700); // r_b_addr
        wr(0x40000408, 0x56885B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 326: /text_model/encoder/layers.7/mlp/fc2/Add (Opcode 0x15)
        wr(0x40000444, 0x001CA00); // r_a_addr
        wr(0x40000448, 0x56885B00); // r_b_addr
        wr(0x40000408, 0x568E9B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 327: /text_model/encoder/layers.7/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x56437F00); // r_a_addr
        wr(0x40000448, 0x568E9B00); // r_b_addr
        wr(0x40000408, 0x5694DB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 328: /text_model/encoder/layers.8/layer_norm1/Sub (Opcode 0x15)
        wr(0x40000444, 0x5694DB00); // r_a_addr
        wr(0x40000448, 0x5694DB00); // r_b_addr
        wr(0x40000408, 0x569B1B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 329: /text_model/encoder/layers.8/layer_norm1/Add (Opcode 0x15)
        wr(0x40000444, 0x569B1B00); // r_a_addr
        wr(0x40000448, 0x56A15B00); // r_b_addr
        wr(0x40000408, 0x56A15F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 330: /text_model/encoder/layers.8/layer_norm1/Div (Opcode 0x15)
        wr(0x40000444, 0x569B1B00); // r_a_addr
        wr(0x40000448, 0x56A15F00); // r_b_addr
        wr(0x40000408, 0x56A79F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 331: /text_model/encoder/layers.8/layer_norm1/Mul (Opcode 0x15)
        wr(0x40000444, 0x56A79F00); // r_a_addr
        wr(0x40000448, 0x001D800); // r_b_addr
        wr(0x40000408, 0x56ADDF00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 332: /text_model/encoder/layers.8/layer_norm1/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x56ADDF00); // r_a_addr
        wr(0x40000448, 0x001DA00); // r_b_addr
        wr(0x40000408, 0x56B41F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 333: /text_model/encoder/layers.8/self_attn/q_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x56BA5F00); // r_a_addr
        wr(0x40000448, 0x305FA00); // r_b_addr
        wr(0x40000408, 0x56BA6300); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 334: /text_model/encoder/layers.8/self_attn/k_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x56BA5F00); // r_a_addr
        wr(0x40000448, 0x309FE00); // r_b_addr
        wr(0x40000408, 0x56BA6700); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 335: /text_model/encoder/layers.8/self_attn/v_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x56BA5F00); // r_a_addr
        wr(0x40000448, 0x30E0200); // r_b_addr
        wr(0x40000408, 0x56BA6B00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 336: /text_model/encoder/layers.8/self_attn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x56B41F00); // r_a_addr
        wr(0x40000448, 0x56BA6F00); // r_b_addr
        wr(0x40000408, 0x56BA7300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 337: /text_model/encoder/layers.8/self_attn/q_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x56B41F00); // r_a_addr
        wr(0x40000448, 0x56BA6300); // r_b_addr
        wr(0x40000408, 0x56F99D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 338: /text_model/encoder/layers.8/self_attn/k_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x56B41F00); // r_a_addr
        wr(0x40000448, 0x56BA6700); // r_b_addr
        wr(0x40000408, 0x56FFDD00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 339: /text_model/encoder/layers.8/self_attn/v_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x56B41F00); // r_a_addr
        wr(0x40000448, 0x56BA6B00); // r_b_addr
        wr(0x40000408, 0x57061D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 340: /text_model/encoder/layers.8/self_attn/q_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x001D400); // r_a_addr
        wr(0x40000448, 0x56F99D00); // r_b_addr
        wr(0x40000408, 0x571F8300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 341: /text_model/encoder/layers.8/self_attn/k_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x001D000); // r_a_addr
        wr(0x40000448, 0x56FFDD00); // r_b_addr
        wr(0x40000408, 0x5725C300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 342: /text_model/encoder/layers.8/self_attn/v_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x001D200); // r_a_addr
        wr(0x40000448, 0x57061D00); // r_b_addr
        wr(0x40000408, 0x572C0300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 343: /text_model/encoder/layers.8/self_attn/Mul (Opcode 0x15)
        wr(0x40000444, 0x571F8300); // r_a_addr
        wr(0x40000448, 0x57324300); // r_b_addr
        wr(0x40000408, 0x57324700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 344: /text_model/encoder/layers.8/self_attn/MatMul (Opcode 0x12)
        wr(0x40000400, 0x57324700); // r_in_addr
        wr(0x40000404, 0x5725C300); // r_w_addr
        wr(0x40000408, 0x57388700); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 345: /text_model/encoder/layers.8/self_attn/Add (Opcode 0x15)
        wr(0x40000444, 0x57388700); // r_a_addr
        wr(0x40000448, 0x3CB1E00); // r_b_addr
        wr(0x40000408, 0x5B83A900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 346: /text_model/encoder/layers.8/self_attn/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x5B83A900); // r_a_addr
        wr(0x40000448, 0x3D18700); // r_b_addr
        wr(0x40000408, 0x5B89E900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 347: /text_model/encoder/layers.8/self_attn/Softmax (Opcode 0x13)
        wr(0x40000444, 0x5B89E900); // r_q_addr
        wr(0x40000448, 0x5B89E900); // r_k_addr
        wr(0x4000044C, 0x5B89E900); // r_v_addr
        wr(0x40000408, 0x5B902900); // r_out_addr
        wr(0x40000450, 4096); // r_seq_len
        wr(0x40000454, 1); // r_num_heads
        wr(0x40000458, 4096); // r_head_dim
        wr(0x40000310, 0x13);

        // Instruction 348: /text_model/encoder/layers.8/self_attn/MatMul_1 (Opcode 0x12)
        wr(0x40000400, 0x5B902900); // r_in_addr
        wr(0x40000404, 0x572C0300); // r_w_addr
        wr(0x40000408, 0x5C902900); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 349: /text_model/encoder/layers.8/self_attn/out_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x60902900); // r_a_addr
        wr(0x40000448, 0x3120600); // r_b_addr
        wr(0x40000408, 0x60902D00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 350: /text_model/encoder/layers.8/self_attn/out_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x5C902900); // r_a_addr
        wr(0x40000448, 0x60902D00); // r_b_addr
        wr(0x40000408, 0x60903100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 351: /text_model/encoder/layers.8/self_attn/out_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x001D600); // r_a_addr
        wr(0x40000448, 0x60903100); // r_b_addr
        wr(0x40000408, 0x60967100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 352: /text_model/encoder/layers.8/Add (Opcode 0x15)
        wr(0x40000444, 0x5694DB00); // r_a_addr
        wr(0x40000448, 0x60967100); // r_b_addr
        wr(0x40000408, 0x609CB100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 353: /text_model/encoder/layers.8/layer_norm2/Sub (Opcode 0x15)
        wr(0x40000444, 0x609CB100); // r_a_addr
        wr(0x40000448, 0x609CB100); // r_b_addr
        wr(0x40000408, 0x60A2F100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 354: /text_model/encoder/layers.8/layer_norm2/Add (Opcode 0x15)
        wr(0x40000444, 0x60A2F100); // r_a_addr
        wr(0x40000448, 0x60A93100); // r_b_addr
        wr(0x40000408, 0x60A93500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 355: /text_model/encoder/layers.8/layer_norm2/Div (Opcode 0x15)
        wr(0x40000444, 0x60A2F100); // r_a_addr
        wr(0x40000448, 0x60A93500); // r_b_addr
        wr(0x40000408, 0x60AF7500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 356: /text_model/encoder/layers.8/layer_norm2/Mul (Opcode 0x15)
        wr(0x40000444, 0x60AF7500); // r_a_addr
        wr(0x40000448, 0x001E600); // r_b_addr
        wr(0x40000408, 0x60B5B500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 357: /text_model/encoder/layers.8/layer_norm2/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x60B5B500); // r_a_addr
        wr(0x40000448, 0x001E800); // r_b_addr
        wr(0x40000408, 0x60BBF500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 358: /text_model/encoder/layers.8/mlp/fc1/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x60C23500); // r_a_addr
        wr(0x40000448, 0x3160A00); // r_b_addr
        wr(0x40000408, 0x60C23900); // r_out_addr
        wr(0x40000450, 2048); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 359: /text_model/encoder/layers.8/mlp/fc1/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x60BBF500); // r_a_addr
        wr(0x40000448, 0x60C23900); // r_b_addr
        wr(0x40000408, 0x60C24100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 360: /text_model/encoder/layers.8/mlp/fc1/Add (Opcode 0x15)
        wr(0x40000444, 0x001DC00); // r_a_addr
        wr(0x40000448, 0x60C24100); // r_b_addr
        wr(0x40000408, 0x60C88100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 2048); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 361: /text_model/encoder/layers.8/mlp/activation_fn/Mul (Opcode 0x15)
        wr(0x40000444, 0x60C88100); // r_a_addr
        wr(0x40000448, 0x60CEC100); // r_b_addr
        wr(0x40000408, 0x60CEC500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 362: /text_model/encoder/layers.8/mlp/activation_fn/Sigmoid (Opcode 0x15)
        wr(0x40000444, 0x60CEC500); // r_a_addr
        wr(0x40000448, 0x60CEC500); // r_b_addr
        wr(0x40000408, 0x60D50500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 363: /text_model/encoder/layers.8/mlp/activation_fn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x60C88100); // r_a_addr
        wr(0x40000448, 0x60D50500); // r_b_addr
        wr(0x40000408, 0x60DB4500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 364: /text_model/encoder/layers.8/mlp/fc2/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x60E18500); // r_a_addr
        wr(0x40000448, 0x3261A00); // r_b_addr
        wr(0x40000408, 0x60E18900); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 365: /text_model/encoder/layers.8/mlp/fc2/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x60DB4500); // r_a_addr
        wr(0x40000448, 0x60E18900); // r_b_addr
        wr(0x40000408, 0x60E18D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 366: /text_model/encoder/layers.8/mlp/fc2/Add (Opcode 0x15)
        wr(0x40000444, 0x001E400); // r_a_addr
        wr(0x40000448, 0x60E18D00); // r_b_addr
        wr(0x40000408, 0x60E7CD00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 367: /text_model/encoder/layers.8/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x609CB100); // r_a_addr
        wr(0x40000448, 0x60E7CD00); // r_b_addr
        wr(0x40000408, 0x60EE0D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 368: /text_model/encoder/layers.9/layer_norm1/Sub (Opcode 0x15)
        wr(0x40000444, 0x60EE0D00); // r_a_addr
        wr(0x40000448, 0x60EE0D00); // r_b_addr
        wr(0x40000408, 0x60F44D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 369: /text_model/encoder/layers.9/layer_norm1/Add (Opcode 0x15)
        wr(0x40000444, 0x60F44D00); // r_a_addr
        wr(0x40000448, 0x60FA8D00); // r_b_addr
        wr(0x40000408, 0x60FA9100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 370: /text_model/encoder/layers.9/layer_norm1/Div (Opcode 0x15)
        wr(0x40000444, 0x60F44D00); // r_a_addr
        wr(0x40000448, 0x60FA9100); // r_b_addr
        wr(0x40000408, 0x6100D100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 371: /text_model/encoder/layers.9/layer_norm1/Mul (Opcode 0x15)
        wr(0x40000444, 0x6100D100); // r_a_addr
        wr(0x40000448, 0x001F200); // r_b_addr
        wr(0x40000408, 0x61071100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 372: /text_model/encoder/layers.9/layer_norm1/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x61071100); // r_a_addr
        wr(0x40000448, 0x001F400); // r_b_addr
        wr(0x40000408, 0x610D5100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 373: /text_model/encoder/layers.9/self_attn/q_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x61139100); // r_a_addr
        wr(0x40000448, 0x3361E00); // r_b_addr
        wr(0x40000408, 0x61139500); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 374: /text_model/encoder/layers.9/self_attn/k_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x61139100); // r_a_addr
        wr(0x40000448, 0x33A2200); // r_b_addr
        wr(0x40000408, 0x61139900); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 375: /text_model/encoder/layers.9/self_attn/v_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x61139100); // r_a_addr
        wr(0x40000448, 0x33E2600); // r_b_addr
        wr(0x40000408, 0x61139D00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 376: /text_model/encoder/layers.9/self_attn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x610D5100); // r_a_addr
        wr(0x40000448, 0x6113A100); // r_b_addr
        wr(0x40000408, 0x6113A500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 377: /text_model/encoder/layers.9/self_attn/q_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x610D5100); // r_a_addr
        wr(0x40000448, 0x61139500); // r_b_addr
        wr(0x40000408, 0x6152CF00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 378: /text_model/encoder/layers.9/self_attn/k_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x610D5100); // r_a_addr
        wr(0x40000448, 0x61139900); // r_b_addr
        wr(0x40000408, 0x61590F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 379: /text_model/encoder/layers.9/self_attn/v_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x610D5100); // r_a_addr
        wr(0x40000448, 0x61139D00); // r_b_addr
        wr(0x40000408, 0x615F4F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 380: /text_model/encoder/layers.9/self_attn/q_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x001EE00); // r_a_addr
        wr(0x40000448, 0x6152CF00); // r_b_addr
        wr(0x40000408, 0x6178B500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 381: /text_model/encoder/layers.9/self_attn/k_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x001EA00); // r_a_addr
        wr(0x40000448, 0x61590F00); // r_b_addr
        wr(0x40000408, 0x617EF500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 382: /text_model/encoder/layers.9/self_attn/v_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x001EC00); // r_a_addr
        wr(0x40000448, 0x615F4F00); // r_b_addr
        wr(0x40000408, 0x61853500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 383: /text_model/encoder/layers.9/self_attn/Mul (Opcode 0x15)
        wr(0x40000444, 0x6178B500); // r_a_addr
        wr(0x40000448, 0x618B7500); // r_b_addr
        wr(0x40000408, 0x618B7900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 384: /text_model/encoder/layers.9/self_attn/MatMul (Opcode 0x12)
        wr(0x40000400, 0x618B7900); // r_in_addr
        wr(0x40000404, 0x617EF500); // r_w_addr
        wr(0x40000408, 0x6191B900); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 385: /text_model/encoder/layers.9/self_attn/Add (Opcode 0x15)
        wr(0x40000444, 0x6191B900); // r_a_addr
        wr(0x40000448, 0x3CB1E00); // r_b_addr
        wr(0x40000408, 0x65DCDB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 386: /text_model/encoder/layers.9/self_attn/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x65DCDB00); // r_a_addr
        wr(0x40000448, 0x3D18700); // r_b_addr
        wr(0x40000408, 0x65E31B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 387: /text_model/encoder/layers.9/self_attn/Softmax (Opcode 0x13)
        wr(0x40000444, 0x65E31B00); // r_q_addr
        wr(0x40000448, 0x65E31B00); // r_k_addr
        wr(0x4000044C, 0x65E31B00); // r_v_addr
        wr(0x40000408, 0x65E95B00); // r_out_addr
        wr(0x40000450, 4096); // r_seq_len
        wr(0x40000454, 1); // r_num_heads
        wr(0x40000458, 4096); // r_head_dim
        wr(0x40000310, 0x13);

        // Instruction 388: /text_model/encoder/layers.9/self_attn/MatMul_1 (Opcode 0x12)
        wr(0x40000400, 0x65E95B00); // r_in_addr
        wr(0x40000404, 0x61853500); // r_w_addr
        wr(0x40000408, 0x66E95B00); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 389: /text_model/encoder/layers.9/self_attn/out_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x6AE95B00); // r_a_addr
        wr(0x40000448, 0x3422A00); // r_b_addr
        wr(0x40000408, 0x6AE95F00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 390: /text_model/encoder/layers.9/self_attn/out_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x66E95B00); // r_a_addr
        wr(0x40000448, 0x6AE95F00); // r_b_addr
        wr(0x40000408, 0x6AE96300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 391: /text_model/encoder/layers.9/self_attn/out_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x001F000); // r_a_addr
        wr(0x40000448, 0x6AE96300); // r_b_addr
        wr(0x40000408, 0x6AEFA300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 392: /text_model/encoder/layers.9/Add (Opcode 0x15)
        wr(0x40000444, 0x60EE0D00); // r_a_addr
        wr(0x40000448, 0x6AEFA300); // r_b_addr
        wr(0x40000408, 0x6AF5E300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 393: /text_model/encoder/layers.9/layer_norm2/Sub (Opcode 0x15)
        wr(0x40000444, 0x6AF5E300); // r_a_addr
        wr(0x40000448, 0x6AF5E300); // r_b_addr
        wr(0x40000408, 0x6AFC2300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 394: /text_model/encoder/layers.9/layer_norm2/Add (Opcode 0x15)
        wr(0x40000444, 0x6AFC2300); // r_a_addr
        wr(0x40000448, 0x6B026300); // r_b_addr
        wr(0x40000408, 0x6B026700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 395: /text_model/encoder/layers.9/layer_norm2/Div (Opcode 0x15)
        wr(0x40000444, 0x6AFC2300); // r_a_addr
        wr(0x40000448, 0x6B026700); // r_b_addr
        wr(0x40000408, 0x6B08A700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 396: /text_model/encoder/layers.9/layer_norm2/Mul (Opcode 0x15)
        wr(0x40000444, 0x6B08A700); // r_a_addr
        wr(0x40000448, 0x0020000); // r_b_addr
        wr(0x40000408, 0x6B0EE700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 397: /text_model/encoder/layers.9/layer_norm2/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x6B0EE700); // r_a_addr
        wr(0x40000448, 0x0020200); // r_b_addr
        wr(0x40000408, 0x6B152700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 398: /text_model/encoder/layers.9/mlp/fc1/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x6B1B6700); // r_a_addr
        wr(0x40000448, 0x3462E00); // r_b_addr
        wr(0x40000408, 0x6B1B6B00); // r_out_addr
        wr(0x40000450, 2048); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 399: /text_model/encoder/layers.9/mlp/fc1/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x6B152700); // r_a_addr
        wr(0x40000448, 0x6B1B6B00); // r_b_addr
        wr(0x40000408, 0x6B1B7300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 400: /text_model/encoder/layers.9/mlp/fc1/Add (Opcode 0x15)
        wr(0x40000444, 0x001F600); // r_a_addr
        wr(0x40000448, 0x6B1B7300); // r_b_addr
        wr(0x40000408, 0x6B21B300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 2048); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 401: /text_model/encoder/layers.9/mlp/activation_fn/Mul (Opcode 0x15)
        wr(0x40000444, 0x6B21B300); // r_a_addr
        wr(0x40000448, 0x6B27F300); // r_b_addr
        wr(0x40000408, 0x6B27F700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 402: /text_model/encoder/layers.9/mlp/activation_fn/Sigmoid (Opcode 0x15)
        wr(0x40000444, 0x6B27F700); // r_a_addr
        wr(0x40000448, 0x6B27F700); // r_b_addr
        wr(0x40000408, 0x6B2E3700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 403: /text_model/encoder/layers.9/mlp/activation_fn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x6B21B300); // r_a_addr
        wr(0x40000448, 0x6B2E3700); // r_b_addr
        wr(0x40000408, 0x6B347700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 404: /text_model/encoder/layers.9/mlp/fc2/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x6B3AB700); // r_a_addr
        wr(0x40000448, 0x3563E00); // r_b_addr
        wr(0x40000408, 0x6B3ABB00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 405: /text_model/encoder/layers.9/mlp/fc2/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x6B347700); // r_a_addr
        wr(0x40000448, 0x6B3ABB00); // r_b_addr
        wr(0x40000408, 0x6B3ABF00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 406: /text_model/encoder/layers.9/mlp/fc2/Add (Opcode 0x15)
        wr(0x40000444, 0x001FE00); // r_a_addr
        wr(0x40000448, 0x6B3ABF00); // r_b_addr
        wr(0x40000408, 0x6B40FF00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 407: /text_model/encoder/layers.9/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x6AF5E300); // r_a_addr
        wr(0x40000448, 0x6B40FF00); // r_b_addr
        wr(0x40000408, 0x6B473F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 408: /text_model/encoder/layers.10/layer_norm1/Sub (Opcode 0x15)
        wr(0x40000444, 0x6B473F00); // r_a_addr
        wr(0x40000448, 0x6B473F00); // r_b_addr
        wr(0x40000408, 0x6B4D7F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 409: /text_model/encoder/layers.10/layer_norm1/Add (Opcode 0x15)
        wr(0x40000444, 0x6B4D7F00); // r_a_addr
        wr(0x40000448, 0x6B53BF00); // r_b_addr
        wr(0x40000408, 0x6B53C300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 410: /text_model/encoder/layers.10/layer_norm1/Div (Opcode 0x15)
        wr(0x40000444, 0x6B4D7F00); // r_a_addr
        wr(0x40000448, 0x6B53C300); // r_b_addr
        wr(0x40000408, 0x6B5A0300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 411: /text_model/encoder/layers.10/layer_norm1/Mul (Opcode 0x15)
        wr(0x40000444, 0x6B5A0300); // r_a_addr
        wr(0x40000448, 0x0020C00); // r_b_addr
        wr(0x40000408, 0x6B604300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 412: /text_model/encoder/layers.10/layer_norm1/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x6B604300); // r_a_addr
        wr(0x40000448, 0x0020E00); // r_b_addr
        wr(0x40000408, 0x6B668300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 413: /text_model/encoder/layers.10/self_attn/q_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x6B6CC300); // r_a_addr
        wr(0x40000448, 0x3664200); // r_b_addr
        wr(0x40000408, 0x6B6CC700); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 414: /text_model/encoder/layers.10/self_attn/k_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x6B6CC300); // r_a_addr
        wr(0x40000448, 0x36A4600); // r_b_addr
        wr(0x40000408, 0x6B6CCB00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 415: /text_model/encoder/layers.10/self_attn/v_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x6B6CC300); // r_a_addr
        wr(0x40000448, 0x36E4A00); // r_b_addr
        wr(0x40000408, 0x6B6CCF00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 416: /text_model/encoder/layers.10/self_attn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x6B668300); // r_a_addr
        wr(0x40000448, 0x6B6CD300); // r_b_addr
        wr(0x40000408, 0x6B6CD700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 417: /text_model/encoder/layers.10/self_attn/q_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x6B668300); // r_a_addr
        wr(0x40000448, 0x6B6CC700); // r_b_addr
        wr(0x40000408, 0x6BAC0100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 418: /text_model/encoder/layers.10/self_attn/k_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x6B668300); // r_a_addr
        wr(0x40000448, 0x6B6CCB00); // r_b_addr
        wr(0x40000408, 0x6BB24100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 419: /text_model/encoder/layers.10/self_attn/v_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x6B668300); // r_a_addr
        wr(0x40000448, 0x6B6CCF00); // r_b_addr
        wr(0x40000408, 0x6BB88100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 420: /text_model/encoder/layers.10/self_attn/q_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0020800); // r_a_addr
        wr(0x40000448, 0x6BAC0100); // r_b_addr
        wr(0x40000408, 0x6BD1E700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 421: /text_model/encoder/layers.10/self_attn/k_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0020400); // r_a_addr
        wr(0x40000448, 0x6BB24100); // r_b_addr
        wr(0x40000408, 0x6BD82700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 422: /text_model/encoder/layers.10/self_attn/v_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0020600); // r_a_addr
        wr(0x40000448, 0x6BB88100); // r_b_addr
        wr(0x40000408, 0x6BDE6700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 423: /text_model/encoder/layers.10/self_attn/Mul (Opcode 0x15)
        wr(0x40000444, 0x6BD1E700); // r_a_addr
        wr(0x40000448, 0x6BE4A700); // r_b_addr
        wr(0x40000408, 0x6BE4AB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 424: /text_model/encoder/layers.10/self_attn/MatMul (Opcode 0x12)
        wr(0x40000400, 0x6BE4AB00); // r_in_addr
        wr(0x40000404, 0x6BD82700); // r_w_addr
        wr(0x40000408, 0x6BEAEB00); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 425: /text_model/encoder/layers.10/self_attn/Add (Opcode 0x15)
        wr(0x40000444, 0x6BEAEB00); // r_a_addr
        wr(0x40000448, 0x3CB1E00); // r_b_addr
        wr(0x40000408, 0x70360D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 426: /text_model/encoder/layers.10/self_attn/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x70360D00); // r_a_addr
        wr(0x40000448, 0x3D18700); // r_b_addr
        wr(0x40000408, 0x703C4D00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 427: /text_model/encoder/layers.10/self_attn/Softmax (Opcode 0x13)
        wr(0x40000444, 0x703C4D00); // r_q_addr
        wr(0x40000448, 0x703C4D00); // r_k_addr
        wr(0x4000044C, 0x703C4D00); // r_v_addr
        wr(0x40000408, 0x70428D00); // r_out_addr
        wr(0x40000450, 4096); // r_seq_len
        wr(0x40000454, 1); // r_num_heads
        wr(0x40000458, 4096); // r_head_dim
        wr(0x40000310, 0x13);

        // Instruction 428: /text_model/encoder/layers.10/self_attn/MatMul_1 (Opcode 0x12)
        wr(0x40000400, 0x70428D00); // r_in_addr
        wr(0x40000404, 0x6BDE6700); // r_w_addr
        wr(0x40000408, 0x71428D00); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 429: /text_model/encoder/layers.10/self_attn/out_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x75428D00); // r_a_addr
        wr(0x40000448, 0x3724E00); // r_b_addr
        wr(0x40000408, 0x75429100); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 430: /text_model/encoder/layers.10/self_attn/out_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x71428D00); // r_a_addr
        wr(0x40000448, 0x75429100); // r_b_addr
        wr(0x40000408, 0x75429500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 431: /text_model/encoder/layers.10/self_attn/out_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0020A00); // r_a_addr
        wr(0x40000448, 0x75429500); // r_b_addr
        wr(0x40000408, 0x7548D500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 432: /text_model/encoder/layers.10/Add (Opcode 0x15)
        wr(0x40000444, 0x6B473F00); // r_a_addr
        wr(0x40000448, 0x7548D500); // r_b_addr
        wr(0x40000408, 0x754F1500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 433: /text_model/encoder/layers.10/layer_norm2/Sub (Opcode 0x15)
        wr(0x40000444, 0x754F1500); // r_a_addr
        wr(0x40000448, 0x754F1500); // r_b_addr
        wr(0x40000408, 0x75555500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 434: /text_model/encoder/layers.10/layer_norm2/Add (Opcode 0x15)
        wr(0x40000444, 0x75555500); // r_a_addr
        wr(0x40000448, 0x755B9500); // r_b_addr
        wr(0x40000408, 0x755B9900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 435: /text_model/encoder/layers.10/layer_norm2/Div (Opcode 0x15)
        wr(0x40000444, 0x75555500); // r_a_addr
        wr(0x40000448, 0x755B9900); // r_b_addr
        wr(0x40000408, 0x7561D900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 436: /text_model/encoder/layers.10/layer_norm2/Mul (Opcode 0x15)
        wr(0x40000444, 0x7561D900); // r_a_addr
        wr(0x40000448, 0x0021A00); // r_b_addr
        wr(0x40000408, 0x75681900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 437: /text_model/encoder/layers.10/layer_norm2/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x75681900); // r_a_addr
        wr(0x40000448, 0x0021C00); // r_b_addr
        wr(0x40000408, 0x756E5900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 438: /text_model/encoder/layers.10/mlp/fc1/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x75749900); // r_a_addr
        wr(0x40000448, 0x3765200); // r_b_addr
        wr(0x40000408, 0x75749D00); // r_out_addr
        wr(0x40000450, 2048); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 439: /text_model/encoder/layers.10/mlp/fc1/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x756E5900); // r_a_addr
        wr(0x40000448, 0x75749D00); // r_b_addr
        wr(0x40000408, 0x7574A500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 440: /text_model/encoder/layers.10/mlp/fc1/Add (Opcode 0x15)
        wr(0x40000444, 0x0021000); // r_a_addr
        wr(0x40000448, 0x7574A500); // r_b_addr
        wr(0x40000408, 0x757AE500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 2048); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 441: /text_model/encoder/layers.10/mlp/activation_fn/Mul (Opcode 0x15)
        wr(0x40000444, 0x757AE500); // r_a_addr
        wr(0x40000448, 0x75812500); // r_b_addr
        wr(0x40000408, 0x75812900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 442: /text_model/encoder/layers.10/mlp/activation_fn/Sigmoid (Opcode 0x15)
        wr(0x40000444, 0x75812900); // r_a_addr
        wr(0x40000448, 0x75812900); // r_b_addr
        wr(0x40000408, 0x75876900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 443: /text_model/encoder/layers.10/mlp/activation_fn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x757AE500); // r_a_addr
        wr(0x40000448, 0x75876900); // r_b_addr
        wr(0x40000408, 0x758DA900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 444: /text_model/encoder/layers.10/mlp/fc2/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x7593E900); // r_a_addr
        wr(0x40000448, 0x3866200); // r_b_addr
        wr(0x40000408, 0x7593ED00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 445: /text_model/encoder/layers.10/mlp/fc2/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x758DA900); // r_a_addr
        wr(0x40000448, 0x7593ED00); // r_b_addr
        wr(0x40000408, 0x7593F100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 446: /text_model/encoder/layers.10/mlp/fc2/Add (Opcode 0x15)
        wr(0x40000444, 0x0021800); // r_a_addr
        wr(0x40000448, 0x7593F100); // r_b_addr
        wr(0x40000408, 0x759A3100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 447: /text_model/encoder/layers.10/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x754F1500); // r_a_addr
        wr(0x40000448, 0x759A3100); // r_b_addr
        wr(0x40000408, 0x75A07100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 448: /text_model/encoder/layers.11/layer_norm1/Sub (Opcode 0x15)
        wr(0x40000444, 0x75A07100); // r_a_addr
        wr(0x40000448, 0x75A07100); // r_b_addr
        wr(0x40000408, 0x75A6B100); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 449: /text_model/encoder/layers.11/layer_norm1/Add (Opcode 0x15)
        wr(0x40000444, 0x75A6B100); // r_a_addr
        wr(0x40000448, 0x75ACF100); // r_b_addr
        wr(0x40000408, 0x75ACF500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 450: /text_model/encoder/layers.11/layer_norm1/Div (Opcode 0x15)
        wr(0x40000444, 0x75A6B100); // r_a_addr
        wr(0x40000448, 0x75ACF500); // r_b_addr
        wr(0x40000408, 0x75B33500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 451: /text_model/encoder/layers.11/layer_norm1/Mul (Opcode 0x15)
        wr(0x40000444, 0x75B33500); // r_a_addr
        wr(0x40000448, 0x0022600); // r_b_addr
        wr(0x40000408, 0x75B97500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 452: /text_model/encoder/layers.11/layer_norm1/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x75B97500); // r_a_addr
        wr(0x40000448, 0x0022800); // r_b_addr
        wr(0x40000408, 0x75BFB500); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 453: /text_model/encoder/layers.11/self_attn/q_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x75C5F500); // r_a_addr
        wr(0x40000448, 0x3966600); // r_b_addr
        wr(0x40000408, 0x75C5F900); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 454: /text_model/encoder/layers.11/self_attn/k_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x75C5F500); // r_a_addr
        wr(0x40000448, 0x39A6A00); // r_b_addr
        wr(0x40000408, 0x75C5FD00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 455: /text_model/encoder/layers.11/self_attn/v_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x75C5F500); // r_a_addr
        wr(0x40000448, 0x39E6E00); // r_b_addr
        wr(0x40000408, 0x75C60100); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 456: /text_model/encoder/layers.11/self_attn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x75BFB500); // r_a_addr
        wr(0x40000448, 0x75C60500); // r_b_addr
        wr(0x40000408, 0x75C60900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 457: /text_model/encoder/layers.11/self_attn/q_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x75BFB500); // r_a_addr
        wr(0x40000448, 0x75C5F900); // r_b_addr
        wr(0x40000408, 0x76053300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 458: /text_model/encoder/layers.11/self_attn/k_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x75BFB500); // r_a_addr
        wr(0x40000448, 0x75C5FD00); // r_b_addr
        wr(0x40000408, 0x760B7300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 459: /text_model/encoder/layers.11/self_attn/v_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x75BFB500); // r_a_addr
        wr(0x40000448, 0x75C60100); // r_b_addr
        wr(0x40000408, 0x7611B300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 460: /text_model/encoder/layers.11/self_attn/q_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0022200); // r_a_addr
        wr(0x40000448, 0x76053300); // r_b_addr
        wr(0x40000408, 0x762B1900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 461: /text_model/encoder/layers.11/self_attn/k_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0021E00); // r_a_addr
        wr(0x40000448, 0x760B7300); // r_b_addr
        wr(0x40000408, 0x76315900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 462: /text_model/encoder/layers.11/self_attn/v_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0022000); // r_a_addr
        wr(0x40000448, 0x7611B300); // r_b_addr
        wr(0x40000408, 0x76379900); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 463: /text_model/encoder/layers.11/self_attn/Mul (Opcode 0x15)
        wr(0x40000444, 0x762B1900); // r_a_addr
        wr(0x40000448, 0x763DD900); // r_b_addr
        wr(0x40000408, 0x763DDD00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 464: /text_model/encoder/layers.11/self_attn/MatMul (Opcode 0x12)
        wr(0x40000400, 0x763DDD00); // r_in_addr
        wr(0x40000404, 0x76315900); // r_w_addr
        wr(0x40000408, 0x76441D00); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 465: /text_model/encoder/layers.11/self_attn/Add (Opcode 0x15)
        wr(0x40000444, 0x76441D00); // r_a_addr
        wr(0x40000448, 0x3CB1E00); // r_b_addr
        wr(0x40000408, 0x7A8F3F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 466: /text_model/encoder/layers.11/self_attn/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x7A8F3F00); // r_a_addr
        wr(0x40000448, 0x3D18700); // r_b_addr
        wr(0x40000408, 0x7A957F00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 467: /text_model/encoder/layers.11/self_attn/Softmax (Opcode 0x13)
        wr(0x40000444, 0x7A957F00); // r_q_addr
        wr(0x40000448, 0x7A957F00); // r_k_addr
        wr(0x4000044C, 0x7A957F00); // r_v_addr
        wr(0x40000408, 0x7A9BBF00); // r_out_addr
        wr(0x40000450, 4096); // r_seq_len
        wr(0x40000454, 1); // r_num_heads
        wr(0x40000458, 4096); // r_head_dim
        wr(0x40000310, 0x13);

        // Instruction 468: /text_model/encoder/layers.11/self_attn/MatMul_1 (Opcode 0x12)
        wr(0x40000400, 0x7A9BBF00); // r_in_addr
        wr(0x40000404, 0x76379900); // r_w_addr
        wr(0x40000408, 0x7B9BBF00); // r_out_addr
        wr(0x40000410, 4096); // r_m
        wr(0x40000414, 4096); // r_k
        wr(0x40000418, 4096); // r_n
        wr(0x4000042C, 0); // r_act_type
        wr(0x40000438, 0x3E000000); // r_in_scale
        wr(0x4000043C, 0x3F800000); // r_w_scale
        wr(0x40000440, 0x3F800000); // r_out_scale
        wr(0x40000310, 0x12);

        // Instruction 469: /text_model/encoder/layers.11/self_attn/out_proj/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x7F9BBF00); // r_a_addr
        wr(0x40000448, 0x3A27200); // r_b_addr
        wr(0x40000408, 0x7F9BC300); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 470: /text_model/encoder/layers.11/self_attn/out_proj/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x7B9BBF00); // r_a_addr
        wr(0x40000448, 0x7F9BC300); // r_b_addr
        wr(0x40000408, 0x7F9BC700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 471: /text_model/encoder/layers.11/self_attn/out_proj/Add (Opcode 0x15)
        wr(0x40000444, 0x0022400); // r_a_addr
        wr(0x40000448, 0x7F9BC700); // r_b_addr
        wr(0x40000408, 0x7FA20700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 472: /text_model/encoder/layers.11/Add (Opcode 0x15)
        wr(0x40000444, 0x75A07100); // r_a_addr
        wr(0x40000448, 0x7FA20700); // r_b_addr
        wr(0x40000408, 0x7FA84700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 473: /text_model/encoder/layers.11/layer_norm2/Sub (Opcode 0x15)
        wr(0x40000444, 0x7FA84700); // r_a_addr
        wr(0x40000448, 0x7FA84700); // r_b_addr
        wr(0x40000408, 0x7FAE8700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 474: /text_model/encoder/layers.11/layer_norm2/Add (Opcode 0x15)
        wr(0x40000444, 0x7FAE8700); // r_a_addr
        wr(0x40000448, 0x7FB4C700); // r_b_addr
        wr(0x40000408, 0x7FB4CB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 475: /text_model/encoder/layers.11/layer_norm2/Div (Opcode 0x15)
        wr(0x40000444, 0x7FAE8700); // r_a_addr
        wr(0x40000448, 0x7FB4CB00); // r_b_addr
        wr(0x40000408, 0x7FBB0B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 476: /text_model/encoder/layers.11/layer_norm2/Mul (Opcode 0x15)
        wr(0x40000444, 0x7FBB0B00); // r_a_addr
        wr(0x40000448, 0x0023400); // r_b_addr
        wr(0x40000408, 0x7FC14B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 477: /text_model/encoder/layers.11/layer_norm2/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x7FC14B00); // r_a_addr
        wr(0x40000448, 0x0023600); // r_b_addr
        wr(0x40000408, 0x7FC78B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 478: /text_model/encoder/layers.11/mlp/fc1/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x7FCDCB00); // r_a_addr
        wr(0x40000448, 0x3A67600); // r_b_addr
        wr(0x40000408, 0x7FCDCF00); // r_out_addr
        wr(0x40000450, 2048); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 479: /text_model/encoder/layers.11/mlp/fc1/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x7FC78B00); // r_a_addr
        wr(0x40000448, 0x7FCDCF00); // r_b_addr
        wr(0x40000408, 0x7FCDD700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 2048); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 480: /text_model/encoder/layers.11/mlp/fc1/Add (Opcode 0x15)
        wr(0x40000444, 0x0022A00); // r_a_addr
        wr(0x40000448, 0x7FCDD700); // r_b_addr
        wr(0x40000408, 0x7FD41700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 2048); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 481: /text_model/encoder/layers.11/mlp/activation_fn/Mul (Opcode 0x15)
        wr(0x40000444, 0x7FD41700); // r_a_addr
        wr(0x40000448, 0x7FDA5700); // r_b_addr
        wr(0x40000408, 0x7FDA5B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 482: /text_model/encoder/layers.11/mlp/activation_fn/Sigmoid (Opcode 0x15)
        wr(0x40000444, 0x7FDA5B00); // r_a_addr
        wr(0x40000448, 0x7FDA5B00); // r_b_addr
        wr(0x40000408, 0x7FE09B00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 483: /text_model/encoder/layers.11/mlp/activation_fn/Mul_1 (Opcode 0x15)
        wr(0x40000444, 0x7FD41700); // r_a_addr
        wr(0x40000448, 0x7FE09B00); // r_b_addr
        wr(0x40000408, 0x7FE6DB00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 484: /text_model/encoder/layers.11/mlp/fc2/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x7FED1B00); // r_a_addr
        wr(0x40000448, 0x3B68600); // r_b_addr
        wr(0x40000408, 0x7FED1F00); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 485: /text_model/encoder/layers.11/mlp/fc2/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x7FE6DB00); // r_a_addr
        wr(0x40000448, 0x7FED1F00); // r_b_addr
        wr(0x40000408, 0x7FED2300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 486: /text_model/encoder/layers.11/mlp/fc2/Add (Opcode 0x15)
        wr(0x40000444, 0x0023200); // r_a_addr
        wr(0x40000448, 0x7FED2300); // r_b_addr
        wr(0x40000408, 0x7FF36300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 512); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 487: /text_model/encoder/layers.11/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x7FA84700); // r_a_addr
        wr(0x40000448, 0x7FF36300); // r_b_addr
        wr(0x40000408, 0x7FF9A300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 488: /text_model/final_layer_norm/Sub (Opcode 0x15)
        wr(0x40000444, 0x7FF9A300); // r_a_addr
        wr(0x40000448, 0x7FF9A300); // r_b_addr
        wr(0x40000408, 0x7FFFE300); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 3); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 489: /text_model/final_layer_norm/Add (Opcode 0x15)
        wr(0x40000444, 0x7FFFE300); // r_a_addr
        wr(0x40000448, 0x80062300); // r_b_addr
        wr(0x40000408, 0x80062700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 490: /text_model/final_layer_norm/Div (Opcode 0x15)
        wr(0x40000444, 0x7FFFE300); // r_a_addr
        wr(0x40000448, 0x80062700); // r_b_addr
        wr(0x40000408, 0x800C6700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 491: /text_model/final_layer_norm/Mul (Opcode 0x15)
        wr(0x40000444, 0x800C6700); // r_a_addr
        wr(0x40000448, 0x0023800); // r_b_addr
        wr(0x40000408, 0x8012A700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 492: /text_model/final_layer_norm/Add_1 (Opcode 0x15)
        wr(0x40000444, 0x8012A700); // r_a_addr
        wr(0x40000448, 0x0023A00); // r_b_addr
        wr(0x40000408, 0x8018E700); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 493: /text_model/Mul_2 (Opcode 0x15)
        wr(0x40000444, 0x80257800); // r_a_addr
        wr(0x40000448, 0x8018E700); // r_b_addr
        wr(0x40000408, 0x80257C00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 494: /text_model/Add_2 (Opcode 0x15)
        wr(0x40000444, 0x3CA9200); // r_a_addr
        wr(0x40000448, 0x80257C00); // r_b_addr
        wr(0x40000408, 0x802BBC00); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 0); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 495: /text_projection/MatMul_quant_scales_mul (Opcode 0x15)
        wr(0x40000444, 0x803E7C00); // r_a_addr
        wr(0x40000448, 0x3C68A00); // r_b_addr
        wr(0x40000408, 0x803E8000); // r_out_addr
        wr(0x40000450, 1024); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 1024); // r_a_len
        wr(0x40000468, 512); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 496: /text_projection/MatMul_quant_output_scale_mul (Opcode 0x15)
        wr(0x40000444, 0x8018E700); // r_a_addr
        wr(0x40000448, 0x803E8000); // r_b_addr
        wr(0x40000408, 0x803E8400); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 2); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 1024); // r_b_len
        wr(0x40000310, 0x15);

        // Instruction 497: /Div (Opcode 0x15)
        wr(0x40000444, 0x803E8400); // r_a_addr
        wr(0x40000448, 0x803E8400); // r_b_addr
        wr(0x40000408, 0x8044C400); // r_out_addr
        wr(0x40000450, 409600); // r_len
        wr(0x40000454, 4); // r_mode
        wr(0x40000464, 409600); // r_a_len
        wr(0x40000468, 409600); // r_b_len
        wr(0x40000310, 0x15);

        std::cout << "[TESTBENCH] Waiting for execution to complete..." << std::endl;
        wait(397600);

        std::cout << "\n====================================================================================================" << std::endl;
        std::cout << "                          SAURIA NPU AUTOMATED GOLDEN ACCURACY CHECKER                             " << std::endl;
        std::cout << "====================================================================================================" << std::endl;
        std::cout << std::left << std::setw(8)  << "Inst #"
                  << std::setw(30) << "Layer / Operator Name"
                  << std::setw(12) << "DRAM Addr"
                  << std::setw(10) << "Size(B)"
                  << std::setw(10) << "MAE"
                  << std::setw(10) << "RMSE"
                  << std::setw(10) << "L_inf"
                  << std::setw(12) << "Cos Sim"
                  << std::setw(8)  << "Status" << std::endl;
        std::cout << "----------------------------------------------------------------------------------------------------" << std::endl;

        std::ifstream f_gold("tools/golden_ref.bin", std::ios::binary);
        if (!f_gold.is_open()) {
            f_gold.open("golden_ref.bin", std::ios::binary);
        }

        int passed_checkpoints = 0;
        int failed_checkpoints = 0;
        {
            uint32_t out_addr = 0x3CA9600;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 1
                      << std::setw(30) << "/text_model/Sub"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3CACC00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 2
                      << std::setw(30) << "/text_model/Add_1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3CAF900;
            size_t num_bytes = 4096;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 3
                      << std::setw(30) << "/text_model/Mul_1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3CB0E00;
            size_t num_bytes = 4096;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 4
                      << std::setw(30) << "/text_model/Mul"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3CB3F00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 5
                      << std::setw(30) << "/text_model/Add"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3CB4300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 6
                      << std::setw(30) << "/text_model/embeddings/Add"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3D18700;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 7
                      << std::setw(30) << "/text_model/Sub_1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3D18B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 8
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3D7CF00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 9
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3DE0F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 10
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3E44F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 11
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3EA8F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 12
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3F0D300;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 13
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3F0D700;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 14
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3F0DB00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 15
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3F0E300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 16
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4300D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 17
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4364D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 18
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x43C8D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 19
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x455F300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 20
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x45C3300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 21
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4627300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 22
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x468B700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 23
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x46EF700;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 24
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x8BA1900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 25
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x8C05900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 26
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x8C69900;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 27
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x9C69900;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 28
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xDC69D00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 29
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xDC6A100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 30
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xDCCE100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 31
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xDD32100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 32
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xDD96100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 33
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xDDFA500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 34
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xDE5E500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 35
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xDEC2500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 36
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xDF26500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 37
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xDF8A900;
            size_t num_bytes = 2048;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 38
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xDF8B100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 39
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xDFEF100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 40
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE053500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 41
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE0B7500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 42
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE11B500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 43
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE17F900;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 44
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE17FD00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 45
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE1E3D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 46
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE247D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 47
                      << std::setw(30) << "/text_model/encoder/layers.0"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE2ABD00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 48
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE310100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 49
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE374100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 50
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE3D8100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 51
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE43C100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 52
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE4A0500;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 53
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE4A0900;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 54
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE4A0D00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 55
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE4A1500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 56
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE893F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 57
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE8F7F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 58
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xE95BF00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 59
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xEAF2500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 60
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xEB56500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 61
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xEBBA500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 62
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xEC1E900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 63
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0xEC82900;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 64
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x13134B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 65
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x13198B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 66
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x131FCB00;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 67
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x141FCB00;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 68
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x181FCF00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 69
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x181FD300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 70
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x18261300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 71
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x182C5300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 72
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x18329300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 73
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x1838D700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 74
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x183F1700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 75
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x18455700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 76
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x184B9700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 77
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x1851DB00;
            size_t num_bytes = 2048;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 78
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x1851E300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 79
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x18582300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 80
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x185E6700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 81
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x1864A700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 82
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x186AE700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 83
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x18712B00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 84
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x18712F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 85
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x18776F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 86
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x187DAF00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 87
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x1883EF00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 88
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x188A3300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 89
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x18907300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 90
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x1896B300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 91
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x189CF300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 92
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x18A33700;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 93
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x18A33B00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 94
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x18A33F00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 95
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x18A34700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 96
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x18E27100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 97
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x18E8B100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 98
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x18EEF100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 99
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x19085700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 100
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x190E9700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 101
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x1914D700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 102
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x191B1B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 103
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x19215B00;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 104
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x1D6C7D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 105
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x1D72BD00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 106
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x1D78FD00;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 107
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x1E78FD00;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 108
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22790100;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 109
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22790500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 110
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x227F4500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 111
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22858500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 112
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x228BC500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 113
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22920900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 114
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22984900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 115
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x229E8900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 116
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22A4C900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 117
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22AB0D00;
            size_t num_bytes = 2048;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 118
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22AB1500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 119
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22B15500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 120
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22B79900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 121
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22BDD900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 122
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22C41900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 123
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22CA5D00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 124
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22CA6100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 125
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22D0A100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 126
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22D6E100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 127
                      << std::setw(30) << "/text_model/encoder/layers.2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22DD2100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 128
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22E36500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 129
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22E9A500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 130
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22EFE500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 131
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22F62500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 132
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22FC6900;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 133
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22FC6D00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 134
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22FC7100;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 135
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x22FC7900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 136
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x233BA300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 137
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2341E300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 138
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x23482300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 139
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x23618900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 140
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2367C900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 141
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x236E0900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 142
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x23744D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 143
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x237A8D00;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 144
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x27C5AF00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 145
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x27CBEF00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 146
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x27D22F00;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 147
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x28D22F00;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 148
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2CD23300;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 149
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2CD23700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 150
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2CD87700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 151
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2CDEB700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 152
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2CE4F700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 153
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2CEB3B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 154
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2CF17B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 155
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2CF7BB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 156
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2CFDFB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 157
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D043F00;
            size_t num_bytes = 2048;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 158
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D044700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 159
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D0A8700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 160
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D10CB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 161
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D170B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 162
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D1D4B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 163
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D238F00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 164
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D239300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 165
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D29D300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 166
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D301300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 167
                      << std::setw(30) << "/text_model/encoder/layers.3"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D365300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 168
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D3C9700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 169
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D42D700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 170
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D491700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 171
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D4F5700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 172
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D559B00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 173
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D559F00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 174
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D55A300;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 175
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D55AB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 176
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D94D500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 177
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2D9B1500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 178
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2DA15500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 179
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2DBABB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 180
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2DC0FB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 181
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2DC73B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 182
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2DCD7F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 183
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x2DD3BF00;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 184
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x321EE100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 185
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x32252100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 186
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x322B6100;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 187
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x332B6100;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 188
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x372B6500;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 189
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x372B6900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 190
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3731A900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 191
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3737E900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 192
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x373E2900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 193
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x37446D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 194
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x374AAD00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 195
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3750ED00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 196
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x37572D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 197
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x375D7100;
            size_t num_bytes = 2048;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 198
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x375D7900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 199
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3763B900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 200
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3769FD00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 201
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x37703D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 202
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x37767D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 203
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x377CC100;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 204
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x377CC500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 205
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x37830500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 206
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x37894500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 207
                      << std::setw(30) << "/text_model/encoder/layers.4"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x378F8500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 208
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3795C900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 209
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x379C0900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 210
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x37A24900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 211
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x37A88900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 212
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x37AECD00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 213
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x37AED100;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 214
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x37AED500;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 215
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x37AEDD00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 216
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x37EE0700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 217
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x37F44700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 218
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x37FA8700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 219
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3813ED00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 220
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x381A2D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 221
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x38206D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 222
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3826B100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 223
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x382CF100;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 224
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3C781300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 225
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3C7E5300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 226
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3C849300;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 227
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x3D849300;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 228
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41849700;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 229
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41849B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 230
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x418ADB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 231
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41911B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 232
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41975B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 233
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x419D9F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 234
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41A3DF00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 235
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41AA1F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 236
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41B05F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 237
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41B6A300;
            size_t num_bytes = 2048;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 238
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41B6AB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 239
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41BCEB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 240
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41C32F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 241
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41C96F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 242
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41CFAF00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 243
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41D5F300;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 244
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41D5F700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 245
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41DC3700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 246
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41E27700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 247
                      << std::setw(30) << "/text_model/encoder/layers.5"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41E8B700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 248
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41EEFB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 249
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41F53B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 250
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x41FB7B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 251
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4201BB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 252
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4207FF00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 253
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x42080300;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 254
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x42080700;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 255
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x42080F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 256
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x42473900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 257
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x424D7900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 258
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4253B900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 259
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x426D1F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 260
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x42735F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 261
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x42799F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 262
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x427FE300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 263
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x42862300;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 264
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x46D14500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 265
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x46D78500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 266
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x46DDC500;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 267
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x47DDC500;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 268
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4BDDC900;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 269
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4BDDCD00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 270
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4BE40D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 271
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4BEA4D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 272
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4BF08D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 273
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4BF6D100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 274
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4BFD1100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 275
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C035100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 276
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C099100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 277
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C0FD500;
            size_t num_bytes = 2048;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 278
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C0FDD00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 279
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C161D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 280
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C1C6100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 281
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C22A100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 282
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C28E100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 283
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C2F2500;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 284
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C2F2900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 285
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C356900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 286
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C3BA900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 287
                      << std::setw(30) << "/text_model/encoder/layers.6"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C41E900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 288
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C482D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 289
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C4E6D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 290
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C54AD00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 291
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C5AED00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 292
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C613100;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 293
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C613500;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 294
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C613900;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 295
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4C614100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 296
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4CA06B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 297
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4CA6AB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 298
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4CACEB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 299
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4CC65100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 300
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4CCC9100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 301
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4CD2D100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 302
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4CD91500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 303
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x4CDF5500;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 304
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x512A7700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 305
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x5130B700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 306
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x5136F700;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 307
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x5236F700;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 308
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x5636FB00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 309
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x5636FF00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 310
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x563D3F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 311
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56437F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 312
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x5649BF00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 313
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56500300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 314
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56564300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 315
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x565C8300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 316
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x5662C300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 317
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56690700;
            size_t num_bytes = 2048;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 318
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56690F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 319
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x566F4F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 320
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56759300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 321
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x567BD300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 322
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56821300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 323
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56885700;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 324
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56885B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 325
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x568E9B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 326
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x5694DB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 327
                      << std::setw(30) << "/text_model/encoder/layers.7"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x569B1B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 328
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56A15F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 329
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56A79F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 330
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56ADDF00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 331
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56B41F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 332
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56BA6300;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 333
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56BA6700;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 334
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56BA6B00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 335
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56BA7300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 336
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56F99D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 337
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x56FFDD00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 338
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x57061D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 339
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x571F8300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 340
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x5725C300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 341
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x572C0300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 342
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x57324700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 343
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x57388700;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 344
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x5B83A900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 345
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x5B89E900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 346
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x5B902900;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 347
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x5C902900;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 348
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60902D00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 349
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60903100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 350
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60967100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 351
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x609CB100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 352
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60A2F100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 353
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60A93500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 354
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60AF7500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 355
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60B5B500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 356
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60BBF500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 357
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60C23900;
            size_t num_bytes = 2048;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 358
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60C24100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 359
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60C88100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 360
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60CEC500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 361
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60D50500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 362
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60DB4500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 363
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60E18900;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 364
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60E18D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 365
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60E7CD00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 366
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60EE0D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 367
                      << std::setw(30) << "/text_model/encoder/layers.8"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60F44D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 368
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x60FA9100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 369
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6100D100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 370
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x61071100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 371
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x610D5100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 372
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x61139500;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 373
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x61139900;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 374
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x61139D00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 375
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6113A500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 376
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6152CF00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 377
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x61590F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 378
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x615F4F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 379
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6178B500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 380
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x617EF500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 381
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x61853500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 382
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x618B7900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 383
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6191B900;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 384
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x65DCDB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 385
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x65E31B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 386
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x65E95B00;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 387
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x66E95B00;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 388
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6AE95F00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 389
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6AE96300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 390
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6AEFA300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 391
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6AF5E300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 392
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6AFC2300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 393
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B026700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 394
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B08A700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 395
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B0EE700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 396
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B152700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 397
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B1B6B00;
            size_t num_bytes = 2048;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 398
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B1B7300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 399
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B21B300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 400
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B27F700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 401
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B2E3700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 402
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B347700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 403
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B3ABB00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 404
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B3ABF00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 405
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B40FF00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 406
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B473F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 407
                      << std::setw(30) << "/text_model/encoder/layers.9"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B4D7F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 408
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B53C300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 409
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B5A0300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 410
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B604300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 411
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B668300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 412
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B6CC700;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 413
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B6CCB00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 414
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B6CCF00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 415
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6B6CD700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 416
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6BAC0100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 417
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6BB24100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 418
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6BB88100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 419
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6BD1E700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 420
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6BD82700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 421
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6BDE6700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 422
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6BE4AB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 423
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x6BEAEB00;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 424
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x70360D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 425
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x703C4D00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 426
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x70428D00;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 427
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x71428D00;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 428
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x75429100;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 429
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x75429500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 430
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7548D500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 431
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x754F1500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 432
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x75555500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 433
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x755B9900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 434
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7561D900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 435
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x75681900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 436
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x756E5900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 437
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x75749D00;
            size_t num_bytes = 2048;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 438
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7574A500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 439
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x757AE500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 440
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x75812900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 441
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x75876900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 442
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x758DA900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 443
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7593ED00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 444
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7593F100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 445
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x759A3100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 446
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x75A07100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 447
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x75A6B100;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 448
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x75ACF500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 449
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x75B33500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 450
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x75B97500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 451
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x75BFB500;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 452
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x75C5F900;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 453
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x75C5FD00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 454
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x75C60100;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 455
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x75C60900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 456
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x76053300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 457
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x760B7300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 458
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7611B300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 459
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x762B1900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 460
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x76315900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 461
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x76379900;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 462
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x763DDD00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 463
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x76441D00;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 464
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7A8F3F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 465
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7A957F00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 466
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7A9BBF00;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 467
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7B9BBF00;
            size_t num_bytes = 16777216;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 468
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7F9BC300;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 469
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7F9BC700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 470
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FA20700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 471
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FA84700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 472
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FAE8700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 473
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FB4CB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 474
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FBB0B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 475
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FC14B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 476
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FC78B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 477
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FCDCF00;
            size_t num_bytes = 2048;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 478
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FCDD700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 479
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FD41700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 480
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FDA5B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 481
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FE09B00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 482
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FE6DB00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 483
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FED1F00;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 484
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FED2300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 485
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FF36300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 486
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FF9A300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 487
                      << std::setw(30) << "/text_model/encoder/layers.1"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x7FFFE300;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 488
                      << std::setw(30) << "/text_model/final_layer_norm"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x80062700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 489
                      << std::setw(30) << "/text_model/final_layer_norm"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x800C6700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 490
                      << std::setw(30) << "/text_model/final_layer_norm"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x8012A700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 491
                      << std::setw(30) << "/text_model/final_layer_norm"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x8018E700;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 492
                      << std::setw(30) << "/text_model/final_layer_norm"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x80257C00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 493
                      << std::setw(30) << "/text_model/Mul_2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x802BBC00;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 494
                      << std::setw(30) << "/text_model/Add_2"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x803E8000;
            size_t num_bytes = 1024;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 495
                      << std::setw(30) << "/text_projection/MatMul_quan"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x803E8400;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 496
                      << std::setw(30) << "/text_projection/MatMul_quan"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        {
            uint32_t out_addr = 0x8044C400;
            size_t num_bytes = 409600;
            std::vector<int8_t> gold_buf(num_bytes, 0);
            if (f_gold.is_open()) {
                f_gold.read(reinterpret_cast<char*>(gold_buf.data()), num_bytes);
            }
            const int8_t* hw_buf = reinterpret_cast<const int8_t*>(&dram[out_addr]);

            double sum_abs_err = 0.0;
            double sum_sq_err = 0.0;
            int max_abs_err = 0;
            double dot_prod = 0.0;
            double norm_hw = 0.0;
            double norm_gold = 0.0;

            for (size_t i = 0; i < num_bytes; i++) {
                int hw_v = static_cast<int>(hw_buf[i]);
                int gold_v = static_cast<int>(gold_buf[i]);
                int diff = std::abs(hw_v - gold_v);
                sum_abs_err += diff;
                sum_sq_err += diff * diff;
                if (diff > max_abs_err) max_abs_err = diff;

                dot_prod += static_cast<double>(hw_v) * static_cast<double>(gold_v);
                norm_hw += static_cast<double>(hw_v) * static_cast<double>(hw_v);
                norm_gold += static_cast<double>(gold_v) * static_cast<double>(gold_v);
            }

            double mae = sum_abs_err / num_bytes;
            double rmse = std::sqrt(sum_sq_err / num_bytes);
            double cos_sim = (norm_hw > 0 && norm_gold > 0) ? (dot_prod / (std::sqrt(norm_hw) * std::sqrt(norm_gold))) : 1.0;
            bool pass = (mae <= 5.0) || (cos_sim >= 0.95);
            if (pass) passed_checkpoints++; else { failed_checkpoints++; errors++; }

            std::cout << std::left << std::setw(8) << 497
                      << std::setw(30) << "/Div"
                      << "0x" << std::hex << std::setw(10) << out_addr << std::dec
                      << std::setw(10) << num_bytes
                      << std::setw(10) << std::fixed << std::setprecision(3) << mae
                      << std::setw(10) << rmse
                      << std::setw(10) << max_abs_err
                      << std::setw(12) << std::setprecision(4) << cos_sim
                      << std::setw(8)  << (pass ? "[PASS]" : "[FAIL]") << std::endl;
        }
        if (f_gold.is_open()) f_gold.close();
        std::cout << "====================================================================================================" << std::endl;
        std::cout << "  VERIFICATION SUMMARY: " << passed_checkpoints << " / " << (passed_checkpoints + failed_checkpoints) 
                  << " Checkpoints PASSED (" << (failed_checkpoints == 0 ? "100.0% SUCCESS" : "VERIFICATION FAILURES DETECTED") << ")" << std::endl;
        std::cout << "====================================================================================================\n" << std::endl;

        std::cout << "[COMPLETED] ONNX Graph Model Execution Finished Successfully!" << std::endl;
        perf.report("vit_b-int8.onnx (32x32)");

        sc_stop();
    }
};

int sc_main(int argc, char **argv)
{
    sc_clock clk("clk", 1.25, SC_NS);
    TbOnnxModel tb("TbOnnxModel_inst");
    tb.i_clk(clk);
    sc_start();
    return tb.errors;
}

# Input workspace

Đặt raw planar YUV420p 8-bit (`*.yuv`) vào thư mục này. File raw không chứa
header nên width, height và số frame phải được truyền riêng cho encoder.

Ví dụ `bus_cif.yuv`: `352x288`, YUV420p 8-bit, 150 frame.

HISTORICAL / INACTIVE UNDER D27
This diagram shows the retired dual-core-chip/NoC direction. It is retained as
evidence only. The active design is one standalone NEO-CORE; read README.md and
neo_core_architecture-d15.drawio. Do not implement from this diagram.

┌────────────────────────── TPU CHIP 0 ──────────────────────────┐
│                                                                │
│  ┌─────── NEO-CORE 0 ───────┐    ┌─────── NEO-CORE 1 ───────┐  │
│  │ Hart 0    mhartid = 0    │    │ Hart 1    mhartid = 1    │  │
│  │ DMA 0                    │    │ DMA 1                    │  │
│  │ MXU + Transform          │    │ MXU + Transform          │  │
│  │ Core SRAM 0              │    │ Core SRAM 1              │  │
│  └────────────┬─────────────┘    └────────────┬─────────────┘  │
│               │                               │                │
│  ┌────────────┴───────────────────────────────┴─────────────┐  │
│  │                    CHIP-LOCAL FABRIC                     │  │
│  │           core <-> core : stays inside, 0 flit           │  │
│  └────────────────────────────┬─────────────────────────────┘  │
│                               │                                │
│  ┌────────────────────────────┴─────────────────────────────┐  │
│  │                 SHARED CHIP NoC ENDPOINT                 │  │
│  │            chunking . aperture check . rebase            │  │
│  └────────────────────────────┬─────────────────────────────┘  │
└───────────────────────────────┼────────────────────────────────┘
                                │  ONE interface per chip
                ┌───────────────┘
                │
                v
   ┌──────────────────────────┐      ┌──────────────────────────┐
   │ Router  (0,0)            ├──────┤ Router  (1,0)            │
   ├──────────────────────────┤      ├──────────────────────────┤
   │ TPU CHIP 0               │      │ HOST                     │
   │ 2 NEO-CORE               │      │                          │
   └────────────┬─────────────┘      └────────────┬─────────────┘
                │                                 │
                │ XY routing                      │
                │                                 │
   ┌────────────┴─────────────┐      ┌────────────┴─────────────┐
   │ Router  (0,1)            ├──────┤ Router  (1,1)            │
   ├──────────────────────────┤      ├──────────────────────────┤
   │ BOOT ROM                 │      │ GLOBAL RAM               │
   │                          │      │                          │
   └──────────────────────────┘      └──────────────────────────┘

     Traffic already running on this bench
       boot           CHIP 0 (0,0) --> BOOT ROM   (0,1)   1 hop
       DMA            CHIP 0 (0,0) <-> GLOBAL RAM (1,1)   2 hops, both ways
       core <-> core                                      0 hop, stays in chip

     Scale-out inside the signed configuration

   ┌──────┬──────┬──────┬──────┐
   │ CHIP │ CHIP │ CHIP │ CHIP │    Manager ID = 3 bit
   │  0   │  1   │  2   │  3   │       -> 8 chip endpoints max
   ├──────┼──────┼──────┼──────┤       -> 16 NEO-CORE
   │ CHIP │ CHIP │ CHIP │ CHIP │
   │  4   │  5   │  6   │  7   │
   ├──────┼──────┼──────┼──────┤    frozen 4x4 = 16 nodes, so
   │ BOOT │GLOBAL│ HOST │      │    8 chips + ROM + RAM + HOST
   │ ROM  │ RAM  │      │      │    all fit inside it
   ├──────┼──────┼──────┼──────┤
   │      │      │      │      │    a 9th chip = new FlooNoC
   │      │      │      │      │    configuration = new RTL
   └──────┴──────┴──────┴──────┘    sign-off
